# 概率论题目测试 Manim 待渲染清单

Generated: 2026-06-21

说明：以下 `manimVideoId` 已在对应 medium/hard 计算题中占位。后续可由 `manim/render.py` 渲染并写入 `content/media.generated.ts`；渲染前前端显示「生成中」占位。

## ch01 随机事件与概率

### quiz-prob-ch01-q008

- 题目：ch01 q008
- 来源：`content/chapters/ch01/1.5.md`
- 主题：全概率公式与贝叶斯公式，三条生产线次品来源追溯
- 脚本要点：
  1. 绘制生产线树图，分支 `B_1,B_2,B_3` 标注 `0.2,0.5,0.3`。
  2. 在各分支附加条件次品率 `0.01,0.02,0.04`。
  3. 动画展示加权乘积 `0.002,0.010,0.012` 汇总为 `P(A)=0.024`。
  4. 高亮第三条分支，形成后验概率 `0.012/0.024=0.5`。

### quiz-prob-ch01-q009

- 题目：ch01 q009
- 来源：`content/chapters/ch01/1.6.md`
- 主题：独立射击中“至少一人命中”与“恰好一人命中”
- 脚本要点：
  1. 绘制 `A/\\bar A` 与 `B/\\bar B` 的 2x2 结果格。
  2. 用独立性填入四格概率 `0.42,0.28,0.18,0.12`。
  3. 为“至少一人命中”遮掉双失误格，得到 `0.88`。
  4. 为“恰好一人命中”高亮两个非对角格，得到 `0.46`。

### quiz-prob-ch01-q010

- 题目：ch01 q010
- 来源：`content/chapters/ch01/1.2.md`
- 主题：德摩根律与加法公式证明
- 脚本要点：
  1. 在全集矩形内绘制两个相交事件圆 `A` 与 `B`。
  2. 高亮 `\\bar A\\bar B`，说明其为 `A\\cup B` 的补。
  3. 动画展开 `P(A\\cup B)=P(A)+P(B)-P(AB)=1-P(AB)`。
  4. 取补得到 `P(\\bar A\\bar B)=P(AB)`。

## ch02 随机变量及其分布

### quiz-prob-ch02-q008

- 题目：ch02 q008
- 来源：`content/chapters/ch01/1.5.md`
- 主题：罕见病检测的贝叶斯后验概率
- 脚本要点：
  1. 用总体条形图拆分 `C=0.001` 与 `\\bar C=0.999`。
  2. 拆出阳性来源：真阳性 `0.00095` 与假阳性 `0.0999`。
  3. 堆叠为阳性总概率 `0.10085`。
  4. 放大阳性人群，显示患病占比 `0.00095/0.10085\\approx0.0094`。

### quiz-prob-ch02-q009

- 题目：ch02 q009
- 来源：`content/chapters/ch02/2.4.md`
- 主题：正态分布标准化与标准正态表
- 脚本要点：
  1. 绘制 `N(3,4)` 正态曲线并标出 `x=1,3,7`。
  2. 动画标准化 `z=(x-3)/2`，端点映射为 `-1` 和 `2`。
  3. 将阴影区间转移到标准正态曲线 `[-1,2]`。
  4. 用对称性计算 `\\Phi(2)-\\Phi(-1)=0.9772-0.1587=0.8185`。

### quiz-prob-ch02-q010

- 题目：ch02 q010
- 来源：`content/chapters/ch01/1.6.md`
- 主题：混联可靠性，串联支路压缩后并联
- 脚本要点：
  1. 绘制两条并联支路：上支路两个串联系统，下支路三个串联系统。
  2. 压缩为 `R_1=0.9^2=0.81` 与 `R_2=0.9^3=0.729`。
  3. 展示系统失效当且仅当两条压缩支路均失效。
  4. 计算 `R=1-(1-R_1)(1-R_2)=0.94851`。

## ch03 多维随机变量及其分布

### quiz-prob-ch03-q008

- 题目：ch03 q008
- 来源：`content/chapters/ch02/2.5.md`
- 主题：非单调变换 `Y=(X-1)^2` 的分布函数法
- 脚本要点：
  1. 显示支撑区间 `(0,2)` 及抛物映射 `y=(x-1)^2`。
  2. 当 `0<y<1` 时，动画展示原像区间 `[1-\\sqrt y,1+\\sqrt y]`。
  3. 在移动区间上积分密度 `x/2`，得到 `F_Y(y)=\\sqrt y`。
  4. 求导得到 `f_Y(y)=1/(2\\sqrt y)`，并标注支撑 `0<y<1`。

### quiz-prob-ch03-q009

- 题目：ch03 q009
- 来源：`content/chapters/ch02/2.5.md`
- 主题：线性变换 `Y=2X+5` 下均匀密度的伸缩
- 脚本要点：
  1. 显示 `X` 在 `(0,10)` 上均匀，密度高 `1/10`。
  2. 用仿射映射 `y=2x+5` 将区间拉伸到 `(5,25)`。
  3. 展示区间长度加倍，密度高度减半。
  4. 得出 `f_Y(y)=1/20`，并说明面积仍为 1。

### quiz-prob-ch03-q010

- 题目：ch03 q010
- 来源：`content/chapters/ch03/3.5.md`
- 主题：两个独立 `U(0,1)` 随机变量之和的卷积
- 脚本要点：
  1. 绘制单位正方形和移动直线 `x+y=z`。
  2. 当 `0<z<1` 时，线段长度由 0 增至 1，积分长度为 `z`。
  3. 当 `1\\le z<2` 时，线段缩短，积分长度为 `2-z`。
  4. 绘制三角形密度并在 `z=1` 标出峰值。

## ch04 随机变量的数字特征

### quiz-prob-ch04-q08

- 题目：ch04 q008
- 来源：`content/chapters/ch03/3.5.md`
- 主题：独立泊松变量可加性
- 脚本要点：
  1. 展示两个独立泊松计数流，参数分别为 2 与 3。
  2. 动画将两个计数流合并为一个参数为 5 的泊松流。
  3. 展示概率质量从 `P(2)`、`P(3)` 汇聚到 `P(5)`。
  4. 高亮结论 `E[Z]=D(Z)=5`。

### quiz-prob-ch04-q09

- 题目：ch04 q009
- 来源：`content/chapters/ch04/4.3.md`
- 主题：协方差与相关系数计算
- 脚本要点：
  1. 从 `E[X],E[Y],E[XY],D(X),D(Y)` 的数据表开始。
  2. 逐步构造 `Cov(X,Y)=E[XY]-E[X]E[Y]`。
  3. 将方差转化为标准差。
  4. 归一化得到 `\\rho=1/2`。

### quiz-prob-ch04-q10

- 题目：ch04 q010
- 来源：`content/chapters/ch03/3.2.md`
- 主题：三角区域联合密度求边缘密度
- 脚本要点：
  1. 绘制三角形区域 `0<x<1,0<y<1-x`，面积为 `1/2`。
  2. 用竖切片显示高度 `1-x` 与联合密度 2。
  3. 积分得到 `f_X(x)=2(1-x)`。
  4. 用非矩形支撑区说明随机变量不独立。

## ch05 大数定律与中心极限定理

### quiz-prob-ch05-q08

- 题目：ch05 q008
- 来源：`content/chapters/ch05/5.1.md`
- 主题：切比雪夫不等式控制样本均值偏差
- 脚本要点：
  1. 显示样本均值方差从 `9` 缩小为 `9/n`。
  2. 将切比雪夫上界写为 `(9/n)/1^2`。
  3. 在数轴上求解 `9/n\\le0.01`。
  4. 标出最小样本量 `n\\ge900`。

### quiz-prob-ch05-q09

- 题目：ch05 q009
- 来源：`content/chapters/ch05/5.3.md`
- 主题：中心极限定理近似求和概率
- 脚本要点：
  1. 将 100 个独立同分布变量聚合为和变量 `S`，均值 200，标准差 30。
  2. 将区间 `[185,215]` 标准化为 `[-0.5,0.5]`。
  3. 在标准正态曲线上高亮中间阴影区。
  4. 计算 `2\\Phi(0.5)-1`。

### quiz-prob-ch05-q10

- 题目：ch05 q010
- 来源：`content/chapters/ch03/3.5.md`
- 主题：独立均匀变量和的三角密度
- 脚本要点：
  1. 绘制单位正方形和对角线条带 `x+y=z`。
  2. 展示 `0<z<1` 时重叠长度为 `z`，`1<z<2` 时为 `2-z`。
  3. 将重叠长度变形为三角密度曲线。
  4. 标出 `E[Z]=1`。

## ch06 样本及抽样分布

### quiz-prob-ch06-q08

- 题目：ch06 q008
- 来源：`content/chapters/ch05/5.3.md`
- 主题：二项分布正态近似
- 脚本要点：
  1. 显示 `B(400,0.25)` 的条形分布，中心在 `np=100`。
  2. 叠加正态曲线 `N(100,75)`。
  3. 将 90 与 110 标准化为约 `\\pm1.155`。
  4. 高亮区间面积约 `0.752`。

### quiz-prob-ch06-q09

- 题目：ch06 q009
- 来源：`content/chapters/ch06/6.3.md`
- 主题：正态总体样本方差的卡方分布
- 脚本要点：
  1. 展示正态样本点围绕样本均值波动。
  2. 展示离差和为 0，说明损失 1 个自由度。
  3. 将 `9S^2/\\sigma^2` 变换为自由度 9 的卡方分布。
  4. 标注这是区间估计与方差检验的基础。

### quiz-prob-ch06-q10

- 题目：ch06 q010
- 来源：`content/chapters/ch06/6.1.md`
- 主题：样本方差无偏性
- 脚本要点：
  1. 分解总体均值周围的总离差平方。
  2. 拆成样本内部离差平方与样本均值偏差项。
  3. 逐项取期望，并代入 `D(\\bar X)=\\sigma^2/n`。
  4. 得出 `E[S^2]=\\sigma^2`，对比用 `n` 作分母的偏差。

## ch07 参数估计

### quiz-prob-ch07-q08

- 题目：ch07 q008
- 来源：`content/chapters/ch07/7.2.md`
- 主题：指数分布参数的极大似然估计
- 脚本要点：
  1. 显示带 `\\lambda` 滑块的指数密度曲线。
  2. 将似然函数构造成乘积模块，再合并为 `L(\\lambda)=\\lambda^n e^{-\\lambda\\sum X_i}`。
  3. 转为对数似然并高亮导数 `n/\\lambda-\\sum X_i`。
  4. 动画显示最大点 `\\hat\\lambda=n/\\sum X_i=1/\\bar X`。

### quiz-prob-ch07-q09

- 题目：ch07 q009
- 来源：`content/chapters/ch05/5.3.md`
- 主题：中心极限定理估计和变量尾概率
- 脚本要点：
  1. 显示 100 个独立噪声条，每个均值 0、方差 4。
  2. 合并为总和变量，标注 `E(S)=0,D(S)=400`。
  3. 将聚合曲线变为 `N(0,400)`，并把阈值 30 标准化为 `z=1.5`。
  4. 阴影右尾，显示 `1-\\Phi(1.5)=0.0668`。

### quiz-prob-ch07-q10

- 题目：ch07 q010
- 来源：`content/chapters/ch07/7.4.md`
- 主题：方差未知时均值置信区间
- 脚本要点：
  1. 对比 `\\sigma` 已知走 Z 路径、未知走 t 路径。
  2. 将 `n=16,\\bar x=172.5,s=6.2,t_{0.005}(15)=2.947` 放入区间公式。
  3. 动画计算半宽 `2.947\\times6.2/4=4.568`。
  4. 在数轴上绘制区间 `(167.93,177.07)`。

## ch08 假设检验

### quiz-prob-ch08-q08

- 题目：ch08 q008
- 来源：`content/chapters/ch08/8.2.md`
- 主题：单侧 t 检验
- 脚本要点：
  1. 展示右侧检验设置 `H_0:\\mu=10,H_1:\\mu>10`。
  2. 从样本均值差和标准误构造统计量 `0.8/0.3=2.667`。
  3. 绘制 t 曲线和右侧拒绝域，临界点 `1.753`。
  4. 将观测统计量移动到拒绝域，给出“拒绝 H0”。

### quiz-prob-ch08-q09

- 题目：ch08 q009
- 来源：`content/chapters/ch07/7.4.md`
- 主题：置信区间与双侧 Z 检验的等价
- 脚本要点：
  1. 用 `\\bar x=52,\\sigma=6,n=36` 构造 95% Z 置信区间。
  2. 动画计算半宽 `1.96\\times1=1.96`。
  3. 绘制区间 `(50.04,53.96)`，标出假设值 50 落在区间外。
  4. 展示等价检验统计量 `Z=2>1.96`。

### quiz-prob-ch08-q10

- 题目：ch08 q010
- 来源：`content/chapters/ch08/8.4.md`
- 主题：两类错误与功效函数
- 脚本要点：
  1. 绘制 `H_0:\\mu=500` 与真实 `\\mu_1=505` 的两条正态曲线，标准误均为 2。
  2. 在 `H_0` 下放置右尾临界均值 `503.29`，对应 `z_{0.05}=1.645`。
  3. 在 `\\mu_1=505` 曲线下，把 `503.29` 左侧染为 `\\beta`，右侧染为功效。
  4. 计算 `\\beta=\\Phi(-0.855)=0.196`，显示功效 `0.804`。
