#!/bin/sh
# Blocks staged files that look like leaked credentials.
# False-positive bypass: SECRET_SCAN_ALLOW=I_UNDERSTAND
set -e
cd "$(git rev-parse --show-toplevel)" || exit 1
exec node --import tsx scripts/check-secrets.ts --staged
