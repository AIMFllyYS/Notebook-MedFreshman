# 更新日志

本项目所有重要变更记录于此。版本遵循语义化版本（SemVer）。

## [0.3.1] - 2026-06-29

本版本在 v0.3.0 基础上完成对话存储性能重构、画布运行时统一、AI 图片生成端到端落地，并新增大学物理第一章完整内容与计费仪表盘。动效层面引入折叠动画、3D 翻卡片、独占手风琴与画布 AI 修订框架，整体交互平滑度显著提升。

### 性能重构（Storage v2 与虚拟化）

- **对话存储迁移至 Storage v2**：聊天历史按会话拆分写入 IndexedDB，替换旧的单一 blob 持久化；附 v2 key helpers 与 debounce 测试，迁移过程可持久化、可回滚
- **ChatThread 虚拟化**：基于 `@tanstack/react-virtual` 实现长会话窗口化渲染，万条消息滚动保持稳定帧率
- **订阅隔离与延迟挂载**：聊天 hook 拆分订阅源，浮窗未可见时延迟挂载，避免后台无效渲染
- **侧边栏与例题懒加载**：例题正文按需加载，侧边栏导航树瘦身，首屏 JS 显著下降
- **搜索引擎升级**：向量检索改为堆 top-K 扫描，BM25 走 chunks-meta 预计算，检索吞吐量提升

### 画布运行时统一重构

- **类型化画布块运行时**：新增 typed canvas block runtime，函数图、SVG、HTML 图表、化学式统一走同一渲染管线
- **统一 AI 修订框架**：画布块支持 AI 修订（shared AI revision frame + unified canvas revision API），修订结果按完整模式持久化
- **渲染器拆分**：canvas frame 与各渲染器解耦，便于扩展新画布类型
- **表达式诊断**：绘图表达式渲染前先诊断，失败时降级为可读错误卡片，不再白屏
- **遗留 SVG 修复路径下线**：旧的 SVG repair 通路移除，统一到新运行时

### AI 图片生成（端到端落地）

- **多组自定义模型与图片生成数据结构**：支持多 API 分组管理自定义模型，新增图片生成专用数据结构
- **图片生成 API 路由**：新增 `/api/image-gen`，复用 OpenAI 兼容端点
- **generateImage 工具与 useImageGen hook**：AI 对话可调用图片生成工具，前端通过 hook 订阅生成状态
- **图片生成 UI 组件**：独立查看器、设置内联编辑表单、模型菜单整合
- **会话持久化与多窗口**：图片生成会话写入 IndexedDB，关闭后可重开；多窗口查看器协同
- **图片搜索配额与画廊**：单会话图片搜索硬上限，超额降级；新增画廊视图汇总历史
- **模型路由归一化**：以 provider apiModelId 与 registryId 双轨寻址，自定义模型按 API 分组作用域隔离，避免跨组串扰

### 计费仪表盘

- **IndexedDB 长期统计**：token 与图片生成计费按会话实时记录，持久化至 IndexedDB
- **API 分组定价模型**：设置面板支持按 API 分组配置定价，含美元汇率换算
- **BillingDashboard**：虚拟化表格 + 3D 翻卡片摘要 + 趋势图表，支持窗口管理器注册与最小化任务栏召回
- **美元汇率设置**：范围校验（clamp）下沉至 store action 单一源，UI 与迁移层共用
- **三项关键修复**：自定义图片模型按 registryId 计费并归类（修正 0 元与错误归类）、趋势图跨年同月冲突按年分桶并切换月聚合、美元汇率 0/NaN 兜底
- **图标复用**：任务栏与溢出菜单复用计费饼图与图片 ImagePlus 图标

### 集中化快捷键系统

- **统一快捷键中枢**：全局快捷键集中注册，支持逐项启停
- **设置面板独占手风琴**：设置分区改为独占手风琴，展开一个自动收起其余，配合折叠动画过渡更顺滑

### 折叠动画与 UI 细节

- **AnimatedCollapse 折叠组件**：SettingsSection 接入平滑的展开收起动画，避免高度突变
- **多彩主题学科配色扩展**：subject accent 由笔记区扩展到整 UI（primary、primary-container、on-primary-container），亮/暗主题分别调色
- **官方品牌 SVG 图标**：替换占位图标，统一视觉
- **ContentPageClient 清理**：移除冗余 data-subject（已由 AppShell 统一注入）

### 大学物理第一章（完整内容上线）

- **OCR 源文件与配图**：第一章课堂录音 OCR 与章节配图入库
- **1.1–1.4 详解**：四节详尽笔记，含 KaTeX 公式与例题
- **例题按小节重分类**：例题与各小节对齐，便于检索
- **题库**：第一章题库上线，含客观题与计算题
- **内容树与侧边栏注册**：大学物理第一章接入内容树与侧边栏导航

### 大学物理（考前模拟与录音例题）

- **考前模拟真题**：收录大学物理考前模拟试卷，附 callout 渲染修复
- **录音例题（rec-01–27）**：27 套录音例题全部完成，含 SVG 图与 LaTeX 转义修复
- **录音例题测验重写**：rec-01–06 测验题重写，覆盖图中渲染场景

### 其他修复与测试

- **AI 伪思考流剔除**：路由层将伪思考内容剔出渲染流，避免污染对话
- **公式步骤 Markdown 复用**：公式步骤走统一 Markdown 渲染，消除渲染偏差
- **会话替换加载与 manifest 写入收敛**：v2 迁移后替换会话正确加载，manifest 写入节流
- **中文命名例题 SSR 加载**：中文文件名例题改走 SSR，规避构建期路径解析
- **笔记选区复制**：浮窗按钮 + Ctrl+C 拦截恢复选区复制
- **图片灯箱点击与拖拽**：图片消息支持点击放大与拖拽到输入框
- **SVG 修复表单收敛**：修复表单限制在 fallback 卡片内
- **测试覆盖**：流式节流与 API payload 窗口性能护栏、虚拟线程渲染、上游错误处理、模型端点校验、quiz 图渲染等

### 安全

- 自定义 API 密钥与图片生成端点均仅服务端处理，不进入前端包；图片生成会话与计费数据本地 IndexedDB，不上传

---

## [0.3.0] - 2026-06-29

### 🪟 统一窗口管理系统（架构重构）

- **统一窗口基础设施**：浮窗全面迁移至统一窗口框架，支持任意层级子窗口管理、生命周期统一调度
- **全局任务栏**：所有浮窗与 Artifact 均可从页面底部任务栏一键召回或切换；面板切换时自动停止生成，避免无效请求
- **全屏状态追踪**：窗口全屏状态统一追踪；Artifact Viewer 全局挂载，跨页面导航不中断
- **全局搜索入口**：任务栏新增全局搜索快捷入口

### 🎨 全局外观定制

- **外观设置面板**：字体大小、行间距、界面主题均可在设置中调节，通过 CSS 层实时生效
- **设置持久化**：外观配置写入 localStorage，刷新后保留

### 💰 Token 看板 & 自定义 API 多模型管理

- **浮窗独立 Token 看板**：每个浮窗单独显示当前会话的 Token 用量与上下文估算圆环，圆环颜色随用量动态变化
- **自定义 API 多模型管理**：在设置中可添加任意 OpenAI 兼容 API 端点和模型，与内置模型统一在模型下拉中选择
- **内置模型定价补全**：所有内置模型均已录入 Token 定价信息，Token 成本估算精确显示

### 🖼 AI 图片搜索（Unsplash 集成）

- **智能图片搜索**：AI 对话中可通过工具调用搜索 Unsplash 高质量图片并插入回复，自动附加版权归属
- **按需启用**：配置 `UNSPLASH_ACCESS_KEY` 即可激活；未配置时自动降级为 Demo 模式（每小时 50 次）

### 🖥 画布与渲染全面修复

- **KaTeX 公式 SVG 白屏根治**：修复公式渲染后画布内容消失的问题
- **SvgDiagram 统一画布**：函数图、SVG 图、SMILES 化学式、HTML-in-canvas 四类内容统一走同一画布渲染路径
- **HTML 图表增强**：AI 生成的 HTML 图表支持下载；思考框跟随画布滚动；新增全屏控件
- **FormulaSteps 解析修复**：公式步骤组件从 rehype-raw 路径迁移至组件解析器，消除渲染偏差
- **AI 消息标签解析统一化**：所有自定义组件标签走统一解析入口，FollowUp 移出渲染流

### 📝 复习板升级

- **划词记录多模式流式处理**：记录板块升级为「解释 / 步骤拆解 / 联系知识点」三种模式，逐字流式输出
- **预览浮窗 UI 全面升级**：统一布局结构、样式规范和模式切换按钮

### 🎯 答题系统

- **答题会话持久化**：答题进度写入 IndexedDB，退出后重新进入自动恢复现场
- **测试题渲染修复**：修复 Markdown 自定义标签在测验组件中不渲染的问题

### 📚 内容扩充：大学英语（CET-4 难度）

- **英语练习板块上线**：新增独立英语科目，Unit 1–8 全部完成
- 每单元包含：原文精读、精讲例题（阅读理解 / 翻译 / 词汇）、随堂测验

### 📚 内容扩充：有机化学

- **测试题全面重写**：全部 14 章测试题由 10 题扩充至 34 题/章，难度梯度更合理
- **华科历年真题**：收录华中科技大学 2021-2022、2023-2024 学年有机化学期末考试真题
- 真题采用卡片式布局，附分子结构图，支持考前模拟训练

### 🗂 导航体验

- **同级文件面板（SiblingFilesPanel）**：笔记页新增同目录文件快速切换面板，浏览相关内容无需返回目录
- **TOC 性能优化**：目录渲染性能提升，修复竞态条件导致的目录跳动问题
- **AI 头部自动隐藏**：发送第一条消息后，AI 面板顶部工具栏自动收起，阅读区更大

### 🎨 视觉细节

- **划词高亮橙色蜡笔风格**：文本划词高亮改为橙色蜡笔风格，视觉辨识度更高
- **Tooltip 优化**：单行 Tooltip 不再截断，圆环图标增加浅色背景轨道
- **会话标题生成优化**：自动标题生成改为轻量方案，不再调用大模型，响应更快

### 安全

- 自定义 API 密钥与内置密钥均仅在服务端处理，不进入前端包；Unsplash Access Key 仅服务端持有

---

## [0.2.1] - 2026-06-26

### 修复（重要）

- **桌面端数据重启丢失（P0）**：旧版本桌面应用每次启动用随机端口,导致页面 origin 每次都变,
  浏览器按 origin 隔离的 IndexedDB 因此每次重启都从空开始——**对话记录 / 导入的技能 / 复习卡全部丢失**
  (API 密钥因单独加密存盘不受影响)。现固定为本地端口 **35349**(网页与桌面同源),数据从此跨重启持久;
  并对端口占用做安全等待与清晰报错,绝不静默改端口而丢数据。

### 新增

- **划词 AI 深度重构**：划词「解释 / 追问」浮窗不再是单独的弱实现，而是**复用完整的右侧 AI 对话引擎**——真实支持工具调用、交互演示、检索引用、图片附件、FollowUp 等全部能力；模型 / 深度思考 / 联网搜索统一在窗口底部，发送↔停止与主对话一致。
- **划词对话本地持久化**：每个划词浮窗就是一段持久会话，关窗不丢；可随时还原。
- **历史记录分栏**：右侧「历史记录」升级为「**对话 / 划词**」两栏；划词栏点击即**还原**旧浮窗。
- **导出所有聊天数据**：AI 设置新增一键导出（主对话 + 划词）为本地 JSON 备份，纯本地、无外发。
- **应用 / 安装器图标**：打包应用沿用站点书本图标；安装器配套精致 LOGO；附 publisher / copyright 元数据。
- **基础代码签名**：对 portable / installer 做自签名 + RFC3161 时间戳（非 CA 受信，仍建议按 SHA256 校验）。

### 优化

- **删除二次确认**：历史记录中删除会话需「删除？/取消」二次确认，防误删。
- **拖拽 / 缩放更顺**：划词浮窗几何改用 rAF 直写样式（零逐帧重渲），调整大小不卡顿。

### 安全

- 划词与主对话同走**服务端** `/api/chat`，API Key 仅后端；导出为本地 Blob，绝不外发；签名私钥仅存本机、不进仓库。

## [0.2.0] - 2026-06-25

### 新增

- **记忆系统（记忆卡片）**：在正文 / AI 输出处划词或右键「记录」，DeepSeek-V4-Flash 后台把*知识点*转成**填空闪卡**、把*题目*转成**问答卡**，自动带章节出处；**先存原文再异步成卡**，绝不丢失。
- **复习板**（`/[subject]/review`）：翻卡复习、左右导航、跳转选卡、删卡、下载（本科目 / 全部）JSON 备份；板内可继续划词记录。
- **首页书架**：根路径改为书架视图，每科一本书，悬浮「开始学习 / 开始复习」；左栏新增「首页」入口。
- **记录预览浮窗**：解析中实时反馈，生成后可保留 / 重做 / 丢弃。
- 划词菜单「举例」替换为「**记录**」；用户消息与全部 AI 输出支持**右键菜单**（复制 / 引用 / 追问 / 记录）。
- AI 对话输入框支持 **Ctrl+V 粘贴 / 拖拽图片**。

### 优化

- **跳卡数字面板**：复习板「选第几张」由下拉框改为**向上弹出的数字方格面板**。
- **卡片可折叠**：预览窗原文区、卡片背面解析支持展开 / 收起（复用统一伸缩组件）。
- **预览浮窗可拖拽缩放**（右下角手柄）。
- **AI 修订卡片**：对已生成的卡片输入一句话指令让 AI 改写，**修订失败自动回滚原卡**（绝不丢内容）。
- **书架封面每科专属配色** + 装饰 SVG 水印（告别统一同色）。

### 安全

- 成卡 / 修订均走**服务端**，API Key 仅后端，绝不进前端包或公开产物。

## [0.1.0] - 2026-06-24

### 新增

- 首个 **Windows 桌面版**（Electron）：多学科笔记 / 正文、AI 助教、Manim 视频、交互演示、内置真实浏览器、题库，本地离线可用。
- 划词追问多开悬浮窗；桌面端真实 Chromium 浏览器（保留登录态 / Cookie）；上下文看板（rAF 拖动 + 实时估算）。
- 3 个 API 密钥用户首启填写，Windows DPAPI 加密存本机，**绝不进包**。

[0.3.1]: https://github.com/AIMFllyYS/Notebook-MedFreshman/releases/tag/v0.3.1
[0.3.0]: https://github.com/AIMFllyYS/Notebook-MedFreshman/releases/tag/v0.3.0
[0.2.1]: https://github.com/AIMFllyYS/Notebook-MedFreshman/releases/tag/v0.2.1
[0.2.0]: https://github.com/AIMFllyYS/Notebook-MedFreshman/releases/tag/v0.2.0
[0.1.0]: https://github.com/AIMFllyYS/Notebook-MedFreshman/releases/tag/v0.1.0
