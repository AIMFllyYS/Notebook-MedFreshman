export const ch08Content = {
  sections: [
    {
      id: "8.1",
      title: "假设检验的基本思想与直觉",
      content: `
## 从"女士品茶"说起

1920年代，英国剑桥大学的下午茶聚会上，发生了一个改变统计学史的故事。一位女士声称，她能够分辨出一杯茶是先加牛奶还是先加茶。在场的统计学家罗纳德·费希尔 (R. A. Fisher) 设计了一个巧妙的实验：他准备了8杯茶，其中4杯先加牛奶、4杯先加茶，随机排列后请女士品尝。

女士全部猜对了8杯中的每一杯。费希尔面临的问题是：**这究竟是女士真有辨别能力，还是仅仅运气好？**

如果女士纯粹靠猜，她全部猜对的概率为：
$$ P(\\text{全部猜对}) = \\frac{\\binom{4}{4}\\binom{4}{0}}{\\binom{8}{4}} = \\frac{1}{70} \\approx 0.0143 $$

这个概率非常小——在一次试验中，小概率事件几乎不可能发生。既然发生了，我们有理由怀疑"女士只是在猜"这一假设，而倾向于相信她确实具有辨别能力。

> **这就是假设检验的核心直觉**：在原假设（女士只是在猜）为真的前提下，如果观测到的事件概率极小，我们就拒绝原假设。

---

## 假设检验的逻辑框架

假设检验 (Hypothesis Testing) 是一种基于样本数据，对总体参数或分布形式做出统计推断的方法。其基本逻辑可以概括为以下四步：

1. **建立假设**：提出原假设 $H_0$（通常表示"无效应"、"无差异"）和备择假设 $H_1$（表示"有效应"、"有差异"）。
2. **选择检验统计量**：构造一个能够反映样本与假设之间偏离程度的统计量。
3. **确定拒绝域**：在 $H_0$ 为真的条件下，找出使该统计量落入概率为 $\\alpha$ 的极端区域。
4. **做出决策**：若样本统计量落入拒绝域，则拒绝 $H_0$；否则，没有足够证据拒绝 $H_0$。

---

## 法庭审判类比：无罪推定

假设检验的逻辑与刑事法庭审判高度相似：

| 法庭审判 | 假设检验 |
| :--- | :--- |
| 被告无罪（原假设） | $H_0$ 为真 |
| 检察官举证 | 收集样本数据 |
| 证据充分则定罪 | 统计量落入拒绝域 |
| 证据不足则无罪释放 | 不拒绝 $H_0$ |
| 冤判无辜（第一类错误） | 弃真错误，概率 $\\alpha$ |
| 放过真凶（第二类错误） | 纳伪错误，概率 $\\beta$ |

> **无罪推定原则**对应于假设检验中的**保护原假设原则**：在没有充分证据之前，我们维持原假设 $H_0$ 不变。只有当样本提供了"足够强"的证据时，才推翻 $H_0$。

---

## 为什么不能"只看数据"？

初学者常问：既然样本均值 $\\bar{x} = 5.2$，总体均值 $\\mu_0 = 5$，两者不相等，为什么不直接说 $\\mu \\neq 5$？

原因在于：**样本具有随机性**。即使总体均值恰好等于 $\\mu_0 = 5$，由于抽样波动，样本均值 $\\bar{X}$ 也几乎不可能精确等于 5。关键问题是：$\\bar{x} = 5.2$ 与 $\\mu_0 = 5$ 之间的差异，是**真实的参数差异**，还是**随机抽样造成的偶然波动**？

假设检验通过概率论的语言来回答这个问题：在 $H_0: \\mu = 5$ 为真的前提下，$|\\bar{X} - 5|$ 超过某个阈值的概率是多少？如果这个概率足够小，我们才认为差异具有统计意义。

---

## 可证伪性与波普尔哲学

科学哲学家卡尔·波普尔 (Karl Popper) 提出：**一个理论如果不能被证伪，就不是科学理论**。

假设检验体现了这一思想：
- 我们**不能**通过有限次试验"证明" $H_0$ 为真（因为总有更大的样本容量可能推翻它）。
- 我们**可以**通过一次试验中的极端数据"证伪" $H_0$（若 $H_0$ 为真，则该数据出现的概率极小）。

> **注意**："不拒绝 $H_0$" 不等于 "接受 $H_0$ 为真"，只表示"当前样本没有提供足够证据推翻 $H_0$"。

---

## 小概率原理

**小概率原理 (Small Probability Principle)** 是假设检验的基石：

> 一个小概率事件在一次试验中几乎不可能发生。若在一次试验中发生了，则原假设 $H_0$ 值得怀疑。

"小概率"的标准由**显著性水平** $\\alpha$ 事先规定，通常取 $\\alpha = 0.05, 0.01$ 或 $0.10$。

---

## 例题 1：抛硬币是否公平？

某赌徒声称他的硬币是公平的（正面概率 $p = 0.5$）。连续抛掷10次，出现7次正面。该硬币是否公平？

**分析**：
- $H_0: p = 0.5$（硬币公平）
- $H_1: p \\neq 0.5$（硬币不公平）

在 $H_0$ 下，正面次数 $X \\sim B(10, 0.5)$：
$$ P(X \\ge 7) = P(X=7) + P(X=8) + P(X=9) + P(X=10) $$
$$ = \\binom{10}{7}(0.5)^{10} + \\binom{10}{8}(0.5)^{10} + \\binom{10}{9}(0.5)^{10} + \\binom{10}{10}(0.5)^{10} $$
$$ \\approx 0.117 + 0.044 + 0.010 + 0.001 = 0.172 $$

由对称性，$P(X \\le 3) = 0.172$，故双侧 $p$ 值 $\\approx 0.344 > 0.05$，**不拒绝** $H_0$。

---

## 例题 2：新药是否有效？

某制药公司声称新药使治愈率从 60% 提高到 80%。对 100 名患者试验，72 人治愈。

**分析**：
- $H_0: p = 0.60$
- $H_1: p > 0.60$

观测治愈率 $\\hat{p} = 0.72$。在 $H_0$ 下：
$$ Z = \\frac{\\hat{p} - 0.60}{\\sqrt{0.60 \\times 0.40 / 100}} = \\frac{0.12}{0.049} \\approx 2.45 $$

$P(Z \\ge 2.45) \\approx 0.007 < 0.05$，**拒绝** $H_0$，有证据表明新药提高了治愈率。

---

## 例题 3：生产线次品率是否超标？

质检标准规定次品率不超过 3%。抽检 200 件，发现 12 件次品。

**分析**：
- $H_0: p \\le 0.03$
- $H_1: p > 0.03$

次品率 $\\hat{p} = 12/200 = 0.06$。在边界 $p = 0.03$ 处：
$$ Z = \\frac{0.06 - 0.03}{\\sqrt{0.03 \\times 0.97 / 200}} \\approx 2.47 $$

$P(Z \\ge 2.47) \\approx 0.007 < 0.05$，**拒绝** $H_0$，生产线次品率可能超标。

---

## 假设检验与估计的关系

| 方法 | 回答的问题 | 输出 |
| :--- | :--- | :--- |
| 点估计 | 参数大约是多少？ | 一个数值 $\\hat{\\theta}$ |
| 区间估计 | 参数在哪个范围内？ | 置信区间 $[L, U]$ |
| 假设检验 | 参数是否等于某特定值？ | 接受/拒绝 $H_0$ |

三者本质相通：置信区间与假设检验对偶——若 $\\mu_0$ 不在 $1-\\alpha$ 置信区间内，则在显著性水平 $\\alpha$ 下拒绝 $H_0: \\mu = \\mu_0$。

<FormulaSteps formula="\\text{拒绝 } H_0: \\mu = \\mu_0 \\iff \\mu_0 \\notin [\\bar{X} - z_{\\alpha/2}\\frac{\\sigma}{\\sqrt{n}}, \\; \\bar{X} + z_{\\alpha/2}\\frac{\\sigma}{\\sqrt{n}}]" steps={[
  "在 $H_0: \\mu = \\mu_0$ 下，检验统计量 $Z = \\frac{\\bar{X} - \\mu_0}{\\sigma/\\sqrt{n}} \\sim N(0,1)$。",
  "双侧检验拒绝域为 $|Z| \\ge z_{\\alpha/2}$，等价于 $|\\bar{X} - \\mu_0| \\ge z_{\\alpha/2} \\cdot \\sigma/\\sqrt{n}$。",
  "整理得 $\\mu_0$ 不在以 $\\bar{X}$ 为中心、半宽为 $z_{\\alpha/2}\\sigma/\\sqrt{n}$ 的区间内。",
  "该区间正是 $\\mu$ 的 $1-\\alpha$ 置信区间，故检验与区间估计对偶。"
]} />

---

## 常见误区

**误区 1**："不拒绝 $H_0$" 等于 "证明了 $H_0$ 为真"。
> 正确理解：只是当前样本证据不足以推翻 $H_0$，可能是样本量太小或效应太弱。

**误区 2**：$p$ 值是 $H_0$ 为真的概率。
> 正确理解：$p$ 值是在 $H_0$ 为真的前提下，观测到当前或更极端数据的概率。

**误区 3**：统计显著等于实际重要。
> 正确理解：大样本下微小差异也可能统计显著，需结合效应量判断实际意义。

---

## 提示

- 假设检验是**概率意义上的决策**，不是逻辑上的绝对证明。
- 始终先明确 $H_0$ 和 $H_1$，再选择检验方法。
- 报告结果时应同时给出检验统计量、$p$ 值和效应量。
- 假设检验的结论是**或然性**的，需结合专业背景解读。

---

## 从直觉到形式化：思维导图

1. **提出疑问** → 参数是否等于某值？两总体是否有差？
2. **建立假设** → $H_0$（保守） vs $H_1$（待证）
3. **选择工具** → Z / t / $\\chi^2$ / F
4. **计算证据** → 检验统计量、p 值
5. **做出决策** → 拒绝或不拒绝 $H_0$（注意：不是"接受"）
6. **解释结论** → 统计显著 + 实际意义

> **第一章至本章的联系**：第 7 章用样本估计参数（点估计、区间估计）；本章在估计基础上，对参数是否取特定值做出**决策**。区间估计给出"参数可能在哪"；假设检验回答"参数是否等于某值"。

<ManimAnimation src="/videos/ch08/hypothesis_intuition.mp4" title="假设检验的基本思想——从猜硬币到统计推断" />

<InteractiveDistribution type="normal" mean={0} stdDev={1} />
`
    },
    {
      id: "8.2",
      title: "原假设与备择假设的建立",
      content: `
## 原假设 $H_0$ 的含义

**原假设 (Null Hypothesis)** $H_0$ 是假设检验中被"保护"的假设，通常表示：
- 无效应、无差异、无变化
- 参数等于某个指定值
- 现状维持不变

$H_0$ 的形式一般是**等号假设**或**包含等号的复合假设**，例如 $\\mu = \\mu_0$、$\\mu \\le \\mu_0$、$\\sigma^2 = \\sigma_0^2$ 等。

> **保护原假设原则**：只有当样本提供了足够强的证据时，才推翻 $H_0$。这类似于"无罪推定"——没有充分证据之前，维持原状。

---

## 备择假设 $H_1$ 的含义

**备择假设 (Alternative Hypothesis)** $H_1$ 是与 $H_0$ 对立的假设，代表我们希望通过样本证据来支持的观点：
- 有效应、有差异、有变化
- 参数不等于、大于或小于某值

$H_1$ 决定了检验是**双侧**还是**单侧**，也决定了拒绝域的形状。

---

## 如何正确建立 $H_0$ 与 $H_1$

### 一般规则

1. **$H_0$ 总是包含等号**：$=$、$\\le$ 或 $\\ge$。
2. **$H_1$ 不含等号**：$\\neq$、$>$ 或 $<$。
3. **将"无变化"放入 $H_0$**：如"新药与旧药无差异"→ $H_0: \\mu_1 = \\mu_2$。
4. **将"研究者想证明的结论"放入 $H_1$**：如"新药优于旧药"→ $H_1: \\mu_{\\text{新}} > \\mu_{\\text{旧}}$。

<FormulaSteps formula="H_0 \\text{ vs } H_1 \\text{ 的建立原则}" steps={[
  "明确研究问题：想证明什么？想比较什么？",
  "将'无效应/无差异/维持现状'表述为 $H_0$。",
  "将'有效应/有差异/发生变化'表述为 $H_1$。",
  "根据 $H_1$ 的方向确定单侧或双侧检验。",
  "检查 $H_0$ 与 $H_1$ 是否互斥且完备（覆盖所有可能）。"
]} />

---

## 双侧检验 vs 单侧检验

### 双侧检验 (Two-tailed Test)

当 $H_1$ 不指定方向时使用：
$$ H_0: \\theta = \\theta_0, \\quad H_1: \\theta \\neq \\theta_0 $$

拒绝域分布在分布的**两侧**，各分配 $\\alpha/2$ 的显著性水平。

**适用场景**：只关心"是否有差异"，不关心方向。例如：两种工艺的产品均值是否不同。

### 单侧检验 (One-tailed Test)

**右侧检验**：
$$ H_0: \\theta \\le \\theta_0, \\quad H_1: \\theta > \\theta_0 $$

**左侧检验**：
$$ H_0: \\theta \\ge \\theta_0, \\quad H_1: \\theta < \\theta_0 $$

拒绝域在分布的**一侧**，全部 $\\alpha$ 分配给该侧。

**适用场景**：有明确的理论或实践方向。例如：检验新药是否**提高**治愈率（右侧）。

---

## 例题 1：检验灯泡平均寿命

某厂声称灯泡平均寿命为 1000 小时。质检部门想检验这一声称是否属实。

**建立假设**：
- $H_0: \\mu = 1000$（厂家声称正确）
- $H_1: \\mu \\neq 1000$（厂家声称不正确）

这是**双侧检验**，因为我们只关心寿命是否等于 1000，不预设高于或低于。

---

## 例题 2：检验药物是否降低血压

研究问题：某降压药是否**显著降低**患者血压？

**建立假设**：
- $H_0: \\mu \\ge \\mu_0$（药物无降压效果，或血压未降低）
- $H_1: \\mu < \\mu_0$（药物有降压效果）

这是**左侧检验**（若以"血压降低"为 $H_1$，则均值减小）。

---

## 例题 3：检验方差是否增大

某精密仪器要求加工误差的标准差不超过 0.5 mm。现抽取样本检验当前加工精度。

**建立假设**：
- $H_0: \\sigma \\le 0.5$（精度合格）
- $H_1: \\sigma > 0.5$（精度不合格，波动过大）

这是关于标准差的**右侧检验**（等价于对 $\\sigma^2$ 的右侧检验）。

---

## 例题 4：两种教学方法比较

比较传统教学（A）与新教学法（B）的效果，问新方法是否**优于**传统方法。

**建立假设**：
- $H_0: \\mu_B - \\mu_A \\le 0$（新方法不优于传统方法）
- $H_1: \\mu_B - \\mu_A > 0$（新方法优于传统方法）

这是**右侧检验**（以均值差 $\\mu_B - \\mu_A$ 为参数）。

---

## 例题 5：检验比例是否达标

某选举规定候选人需获得超过 50% 的选票才能当选。问当前得票率是否达标？

**建立假设**：
- $H_0: p \\le 0.50$（未达标）
- $H_1: p > 0.50$（达标）

这是关于比例的**右侧检验**。

---

## 常见错误

| 错误类型 | 错误示例 | 正确做法 |
| :--- | :--- | :--- |
| $H_0$ 不含等号 | $H_0: \\mu < 100$ | $H_0: \\mu \\ge 100$ 或 $\\mu = 100$ |
| $H_0$ 与 $H_1$ 重叠 | $H_0: \\mu \\le 5$, $H_1: \\mu \\ge 3$ | 确保互斥且完备 |
| 方向搞反 | 想证 $\\mu > 5$ 却设 $H_1: \\mu < 5$ | $H_1$ 应含想证明的方向 |
| 双侧/单侧混淆 | 有方向却用双侧 | 有明确方向用单侧 |

---

## 单侧 vs 双侧的选择策略

> **提示**：若研究问题有明确的"大于"或"小于"方向，优先使用单侧检验，功效更高。若仅问"是否不同"，使用双侧检验。

| 情况 | 推荐类型 |
| :--- | :--- |
| "是否等于 $\\mu_0$？" | 双侧 |
| "是否大于 $\\mu_0$？" | 右侧（$H_1: \\mu > \\mu_0$） |
| "是否小于 $\\mu_0$？" | 左侧（$H_1: \\mu < \\mu_0$） |
| 无先验方向，探索性研究 | 双侧 |
| 验证性研究，有理论预期 | 单侧 |

---

## 参数空间与假设的完备性

设参数 $\\theta$ 的参数空间为 $\\Theta$。$H_0$ 和 $H_1$ 必须满足：
1. **互斥**：$H_0 \\cap H_1 = \\emptyset$
2. **完备**：$H_0 \\cup H_1 = \\Theta$（或至少覆盖我们关心的区域）

例如，对正态均值 $\\mu$：
- 双侧：$H_0: \\mu = 5$，$H_1: \\mu \\neq 5$，参数空间 $\\mathbb{R}$ 被完全划分。
- 单侧：$H_0: \\mu \\le 5$，$H_1: \\mu > 5$，同样完备。

---

## 与置信区间的一致性

假设的建立应与后续推断方法一致：
- 若 $H_1: \\mu > \\mu_0$，拒绝域在分布右尾，对应单侧上置信限。
- 若 $H_1: \\mu \\neq \\mu_0$，拒绝域在双尾，对应双侧置信区间。

---

## 考研真题风格补充例题

**例题 6**：电子元件额定电阻 100 Ω，检验批次平均电阻是否**偏高**。

- $H_0: \\mu \\le 100$，$H_1: \\mu > 100$（右侧）

**例题 7**：比较两种肥料，无先验方向。

- $H_0: \\mu_1 = \\mu_2$，$H_1: \\mu_1 \\neq \\mu_2$（双侧）

**例题 8**：检验新工艺是否**降低**次品率 $p$。

- $H_0: p \\ge p_0$，$H_1: p < p_0$（左侧）

<ManimAnimation src="/videos/ch08/hypothesis_setup.mp4" title="原假设与备择假设的建立过程动画" />

<InteractiveDistribution type="normal" mean={0} stdDev={1} />
`
    },
    {
      id: "8.3",
      title: "显著性水平与小概率原理",
      content: `
## 显著性水平 $\\alpha$ 的定义

**显著性水平 (Significance Level)** $\\alpha$ 是事先规定的、允许犯**第一类错误**（弃真错误）的最大概率：

$$ \\alpha = P(\\text{拒绝 } H_0 \\mid H_0 \\text{ 为真}) $$

换言之，$\\alpha$ 是当 $H_0$ 实际上为真时，我们错误地拒绝它的概率上限。通常取 $\\alpha = 0.05$（5%）、$0.01$（1%）或 $0.10$（10%）。

---

## 为什么通常取 $\\alpha = 0.05$？

这一惯例源于费希尔和奈曼-皮尔逊 (Neyman-Pearson) 的工作：

| $\\alpha$ 值 | 含义 | 适用场景 |
| :---: | :--- | :--- |
| 0.10 | 较宽松，10% 弃真风险 | 探索性研究、 pilot 研究 |
| 0.05 | 常规标准，5% 弃真风险 | 大多数科研论文、工业应用 |
| 0.01 | 较严格，1% 弃真风险 | 医学试验、高风险决策 |
| 0.001 | 极严格 | 粒子物理、基因组学 |

> **注意**：$\\alpha = 0.05$ 并非"自然法则"，而是人为约定的权衡。选择 $\\alpha$ 应结合问题的实际后果。

---

## $\\alpha$ 与证据"强度"的关系

$\\alpha$ 越小，拒绝 $H_0$ 所需的证据越强：

- $\\alpha = 0.05$：检验统计量需落入概率 5% 的极端区域
- $\\alpha = 0.01$：需落入概率 1% 的极端区域
- $\\alpha = 0.10$：只需落入概率 10% 的极端区域

**同一组数据**，在 $\\alpha = 0.10$ 下可能拒绝 $H_0$，在 $\\alpha = 0.01$ 下可能不拒绝。

---

## 小概率原理的严格表述

**小概率原理**：设某事件 $A$ 在原假设 $H_0$ 下发生的概率 $P(A \\mid H_0) = p$。若 $p \\le \\alpha$，则在一次试验中观测到 $A$ 时，有理由拒绝 $H_0$。

<FormulaSteps formula="P(\\text{拒绝 } H_0 \\mid H_0 \\text{ 真}) = \\alpha" steps={[
  "在 $H_0$ 为真的条件下，确定检验统计量 $T$ 的分布。",
  "根据 $\\alpha$ 确定拒绝域 $W$，使得 $P(T \\in W \\mid H_0) = \\alpha$。",
  "双侧检验：$P(T \\le c_1 \\mid H_0) + P(T \\ge c_2 \\mid H_0) = \\alpha$，各 $\\alpha/2$。",
  "单侧检验：$P(T \\ge c \\mid H_0) = \\alpha$ 或 $P(T \\le c \\mid H_0) = \\alpha$。",
  "若观测值 $t_{\\text{obs}} \\in W$，则拒绝 $H_0$。"
]} />

---

## 例题 1：选择 $\\alpha$ 的考量

某药厂检验新药是否比安慰剂更有效。若错误上市无效药物（弃真：实际上无效却认为有效），可能导致患者延误治疗；若错误拒绝有效药物（纳伪），可能错失治疗机会。

- 若更担心弃真（将无效药当有效），取较小 $\\alpha$，如 0.01。
- 若更担心纳伪（错过有效药物），可适当增大 $\\alpha$ 或增大样本量以提高功效。

---

## 例题 2：不同 $\\alpha$ 下的决策

设 $Z$ 检验得到 $Z_{\\text{obs}} = 2.0$，检验 $H_0: \\mu = 10$ vs $H_1: \\mu \\neq 10$。

- $\\alpha = 0.10$：$z_{0.05} = 1.645$，$|Z| = 2.0 > 1.645$，**拒绝** $H_0$
- $\\alpha = 0.05$：$z_{0.025} = 1.96$，$|Z| = 2.0 > 1.96$，**拒绝** $H_0$
- $\\alpha = 0.01$：$z_{0.005} = 2.576$，$|Z| = 2.0 < 2.576$，**不拒绝** $H_0$

同一数据，结论随 $\\alpha$ 变化。

---

## 例题 3：单侧检验中的 $\\alpha$

检验 $H_0: \\mu \\le 100$ vs $H_1: \\mu > 100$，$\\bar{x} = 105$，$\\sigma = 10$，$n = 25$。

$$ Z = \\frac{105 - 100}{10/\\sqrt{25}} = 2.5 $$

$\\alpha = 0.05$ 时，$z_{0.05} = 1.645$，$Z = 2.5 > 1.645$，拒绝 $H_0$。
$P(Z \\ge 2.5) \\approx 0.0062$，即 $p = 0.0062 < 0.05$。

---

## 显著性水平与置信水平的关系

置信水平 $1 - \\alpha$ 与显著性水平 $\\alpha$ 互补：

- 95% 置信区间 ↔ $\\alpha = 0.05$ 的双侧检验
- 99% 置信区间 ↔ $\\alpha = 0.01$ 的双侧检验

若 $\\mu_0$ 不在 $\\mu$ 的 $1-\\alpha$ 置信区间内，则在水平 $\\alpha$ 下拒绝 $H_0: \\mu = \\mu_0$。

<FormulaSteps formula="1 - \\alpha = P(\\mu_0 \\in [\\bar{X} - z_{\\alpha/2}\\frac{\\sigma}{\\sqrt{n}}, \\bar{X} + z_{\\alpha/2}\\frac{\\sigma}{\\sqrt{n}}])" steps={[
  "置信区间：以 $\\bar{X}$ 为中心，覆盖 $\\mu$ 真值的区间，覆盖概率为 $1-\\alpha$。",
  "假设检验：在 $H_0: \\mu = \\mu_0$ 下，拒绝域概率为 $\\alpha$。",
  "对偶关系：$\\mu_0$ 落在置信区间外 $\\iff$ 拒绝 $H_0: \\mu = \\mu_0$。",
  "置信水平 $1-\\alpha$ 与显著性水平 $\\alpha$ 之和为 1。"
]} />

---

## 实际推断原理

**实际推断原理**（小概率原理的另一种表述）：

> 若事件 $A$ 在假设 $H$ 下的概率 $P(A \\mid H) \\le \\alpha$，则在一次试验中若 $A$ 发生，应拒绝假设 $H$。

这是假设检验从"概率计算"到"统计决策"的桥梁。

---

## 常见误区

**误区 1**：$\\alpha$ 是 $H_0$ 为假的概率。
> 正确：$\\alpha$ 是 $H_0$ 为真时拒绝它的概率。

**误区 2**：$\\alpha$ 越小越好。
> 正确：$\\alpha$ 过小会降低检验功效（增大 $\\beta$），需权衡。

**误区 3**：$p$ 值就是 $\\alpha$。
> 正确：$p$ 值由数据决定；$\\alpha$ 由研究者事先设定。当 $p \\le \\alpha$ 时拒绝 $H_0$。

---

## 提示

- 在报告结果前明确写出 $\\alpha$ 的选择及理由。
- 多个检验时考虑 Bonferroni 校正：$\\alpha_{\\text{adj}} = \\alpha / m$（$m$ 为检验次数）。
- 探索性分析可用较大 $\\alpha$，确证性分析用常规 0.05 或更严。

<ManimAnimation src="/videos/ch08/significance_level.mp4" title="显著性水平α的几何含义动画" />

<InteractiveDistribution type="normal" mean={0} stdDev={1} />
`
    },
    {
      id: "8.4",
      title: "两类错误的深入理解",
      content: `
## 两类错误的定义

由于样本的随机性，假设检验的决策可能出错：

| 决策 \\\\ 真实状态 | $H_0$ 为真 | $H_0$ 为假 ($H_1$ 为真) |
| :---: | :---: | :---: |
| **不拒绝 $H_0$** | 正确 (概率 $1-\\alpha$) | **第二类错误** $\\beta$ (纳伪) |
| **拒绝 $H_0$** | **第一类错误** $\\alpha$ (弃真) | 正确 (概率 $1-\\beta$，功效) |

---

## 第一类错误（弃真错误）

**第一类错误 (Type I Error)**：当 $H_0$ 实际上为真时，错误地拒绝 $H_0$。

$$ \\alpha = P(\\text{拒绝 } H_0 \\mid H_0 \\text{ 为真}) $$

**典型例子**：
- **医学**：健康人被误诊为患病（假阳性）
- **司法**：无辜者被判有罪
- **质检**：合格产品被误判为不合格
- **药物试验**：无效药物被误认为有效

---

## 第二类错误（纳伪错误）

**第二类错误 (Type II Error)**：当 $H_0$ 实际上为假（$H_1$ 为真）时，错误地不拒绝 $H_0$。

$$ \\beta = P(\\text{不拒绝 } H_0 \\mid H_1 \\text{ 为真}) $$

**典型例子**：
- **医学**：患者被漏诊（假阴性）
- **司法**：罪犯被无罪释放
- **质检**：不合格产品流入市场
- **药物试验**：有效药物被误认为无效

---

## 检验功效

**检验功效 (Power of a Test)**：当 $H_0$ 为假时，正确拒绝 $H_0$ 的概率：

$$ \\text{Power} = 1 - \\beta = P(\\text{拒绝 } H_0 \\mid H_1 \\text{ 为真}) $$

功效越高，发现真实效应的能力越强。通常希望功效 $\\ge 0.80$。

---

## $\\alpha$ 与 $\\beta$ 的权衡

在样本量 $n$ 固定时，$\\alpha$ 与 $\\beta$ **此消彼长**：

- 减小 $\\alpha$ → 拒绝域缩小 → 更难拒绝 $H_0$ → 当 $H_1$ 为真时更易不拒绝 → $\\beta$ 增大
- 增大 $\\alpha$ → 拒绝域扩大 → 更易拒绝 $H_0$ → $\\beta$ 减小

**唯一同时减小 $\\alpha$ 和 $\\beta$ 的方法**：**增加样本量 $n$**。

<FormulaSteps formula="\\alpha \\downarrow \\Rightarrow \\beta \\uparrow \\quad (n \\text{ 固定})" steps={[
  "减小 $\\alpha$ 意味着拒绝域 $W$ 变小（临界值更极端）。",
  "拒绝域变小 → 非拒绝域变大。",
  "当 $H_1$ 为真时，检验统计量更可能落在非拒绝域内。",
  "因此 $\\beta = P(\\text{不拒绝 } H_0 \\mid H_1 \\text{ 真})$ 增大。",
  "增加 $n$ 可缩小抽样分布的离散程度，同时减小 $\\alpha$ 和 $\\beta$。"
]} />

---

## 几何解释

考虑 $H_0: \\mu = \\mu_0$ vs $H_1: \\mu = \\mu_1$（$\\mu_1 > \\mu_0$）的 Z 检验：

- 在 $H_0$ 下，$\\bar{X} \\sim N(\\mu_0, \\sigma^2/n)$
- 在 $H_1$ 下，$\\bar{X} \\sim N(\\mu_1, \\sigma^2/n)$

两分布重叠区域中：
- 拒绝域在 $H_0$ 分布下的面积 = $\\alpha$
- 非拒绝域在 $H_1$ 分布下的面积 = $\\beta$

<ManimAnimation src="/videos/ch08/hypothesis_errors.mp4" title="第一类与第二类错误的几何权衡动画" />

<InteractiveDistribution type="normal" mean={0} stdDev={1} />

---

## 例题 1：计算 $\\beta$（Z 检验）

$H_0: \\mu = 100$ vs $H_1: \\mu = 110$，$\\sigma = 20$，$n = 25$，$\\alpha = 0.05$（右侧检验）。

拒绝域：$Z = \\frac{\\bar{X} - 100}{20/5} \\ge 1.645$，即 $\\bar{X} \\ge 100 + 1.645 \\times 4 = 106.58$。

在 $H_1: \\mu = 110$ 下：
$$ \\beta = P(\\bar{X} < 106.58 \\mid \\mu = 110) = P\\left(Z < \\frac{106.58 - 110}{4}\\right) = P(Z < -0.855) \\approx 0.39 $$

功效 $= 1 - 0.39 = 0.61$。

---

## 例题 2：医学检测的权衡

某疾病患病率 1%，检测灵敏度 99%（$1-\\beta$），特异度 95%（$1-\\alpha$）。

- 特异度 95% → $\\alpha = 0.05$：健康人中 5% 假阳性
- 灵敏度 99% → $\\beta = 0.01$：患者中 1% 假阴性

若更怕漏诊（纳伪），应提高灵敏度；若更怕误诊（弃真），应提高特异度。

---

## 例题 3：样本量对 $\\beta$ 的影响

沿用例题 1，若 $n = 100$（$\\sigma/\\sqrt{n} = 2$）：

拒绝域 $\\bar{X} \\ge 100 + 1.645 \\times 2 = 103.29$。

$$ \\beta = P(\\bar{X} < 103.29 \\mid \\mu = 110) = P(Z < -3.355) \\approx 0.0004 $$

$n$ 从 25 增至 100，$\\beta$ 从 0.39 降至约 0.0004，功效大幅提升。

---

## 奈曼-皮尔逊原则

**显著性检验原则**：在控制 $\\alpha$ 不超过指定水平的前提下，选择使 $\\beta$ 最小（功效最大）的检验。

这引出了**最优检验**理论，如似然比检验在适当条件下是最优的。

---

## 常见误区

**误区 1**：$\\alpha$ 和 $\\beta$ 可以同时任意小。
> 正确：$n$ 固定时二者此消彼长，只能增大 $n$ 同时减小。

**误区 2**：不拒绝 $H_0$ 意味着 $H_0$ 为真。
> 正确：可能是 $H_0$ 为真，也可能是 $H_1$ 为真但功效不足（$\\beta$ 大）。

**误区 3**：$p$ 值小则 $\\beta$ 小。
> 正确：$p$ 值与 $\\alpha$ 比较；$\\beta$ 取决于 $H_1$ 下的效应大小和 $n$。

---

## 提示

- 设计研究时同时考虑 $\\alpha$ 和功效 $1-\\beta$，必要时进行**样本量计算**（见 8.13 节）。
- 报告时说明两类错误的实际后果，帮助读者理解 $\\alpha$ 的选择。

---

## 例题 4：$\\alpha$ 与 $\\beta$ 的数值权衡

设 $H_0: \\mu = 100$ vs $H_1: \\mu = 110$，$\\sigma = 20$，$n = 25$，右侧检验。

| $\\alpha$ | 临界值 $\\bar{x}_c$ | $\\beta$ | 功效 $1-\\beta$ |
| :---: | :---: | :---: | :---: |
| 0.10 | 105.04 | 0.22 | 0.78 |
| 0.05 | 106.58 | 0.39 | 0.61 |
| 0.01 | 108.65 | 0.66 | 0.34 |

$\\alpha$ 减小 → 拒绝域缩小 → $\\beta$ 增大 → 功效下降。唯一同时改善两者的方法是**增大 $n$**。

---

## 不同领域的错误代价

| 领域 | 第一类错误（弃真） | 第二类错误（纳伪） |
| :--- | :--- | :--- |
| 医学诊断 | 健康人误诊为病 | 患者漏诊 |
| 质量控制 | 合格品判为不合格 | 不合格品流入市场 |
| 新药试验 | 无效药判为有效 | 有效药判为无效 |
| 司法 | 无辜者被判有罪 | 罪犯被释放 |
`
    },
    {
      id: "8.5",
      title: "检验统计量与拒绝域",
      content: `
## 检验统计量的概念

**检验统计量 (Test Statistic)** 是在 $H_0$ 为真时，其抽样分布已知的统计量，用于衡量样本与 $H_0$ 的偏离程度。

构造原则：
1. 与待检验参数有关
2. 在 $H_0$ 下分布已知（或可渐近确定）
3. 偏离 $H_0$ 时，统计量倾向于取极端值

常见检验统计量：$Z$、$t$、$\\chi^2$、$F$ 等。

---

## 拒绝域与接受域

**拒绝域 (Rejection Region)** $W$：当检验统计量的观测值落入 $W$ 时，拒绝 $H_0$。

**接受域 (Acceptance Region)** $\\bar{W}$：当观测值落入 $\\bar{W}$ 时，不拒绝 $H_0$。$W \\cup \\bar{W}$ 为样本空间，$W \\cap \\bar{W} = \\emptyset$。

在 $H_0$ 下：$P(T \\in W) = \\alpha$，$P(T \\in \\bar{W}) = 1 - \\alpha$。

---

## 临界值

**临界值 (Critical Value)** 是拒绝域的边界。例如，双侧 Z 检验中，临界值为 $\\pm z_{\\alpha/2}$；拒绝域为 $|Z| \\ge z_{\\alpha/2}$。

| 检验类型 | 双侧临界值 | 右侧临界值 | 左侧临界值 |
| :--- | :--- | :--- | :--- |
| Z 检验 | $\\pm z_{\\alpha/2}$ | $z_\\alpha$ | $-z_\\alpha$ |
| t 检验 | $\\pm t_{\\alpha/2}(n-1)$ | $t_\\alpha(n-1)$ | $-t_\\alpha(n-1)$ |

---

## 单侧 vs 双侧拒绝域

**双侧**：拒绝域在分布两侧，各 $\\alpha/2$。
$$ W = \\{ |T| \\ge c \\}, \\quad P(|T| \\ge c \\mid H_0) = \\alpha $$

**右侧**：拒绝域在右尾。
$$ W = \\{ T \\ge c \\}, \\quad P(T \\ge c \\mid H_0) = \\alpha $$

**左侧**：拒绝域在左尾。
$$ W = \\{ T \\le c \\}, \\quad P(T \\le c \\mid H_0) = \\alpha $$

<FormulaSteps formula="W = \\{ T \\ge t_\\alpha(n-1) \\} \\text{ (右侧 t 检验)}" steps={[
  "确定 $H_0$ 和 $H_1$，选择 t 检验。",
  "在 $H_0$ 下，$t = \\frac{\\bar{X} - \\mu_0}{S/\\sqrt{n}} \\sim t(n-1)$。",
  "查 t 分布表得右尾 $\\alpha$ 分位数 $t_\\alpha(n-1)$。",
  "拒绝域 $W = \\{ t \\ge t_\\alpha(n-1) \\}$。",
  "若 $t_{\\text{obs}} \\ge t_\\alpha(n-1)$，拒绝 $H_0$。"
]} />

---

## 假设检验的五步法（详细）

### 第一步：建立假设

根据实际问题写出 $H_0$ 和 $H_1$，确定单侧或双侧。

### 第二步：选择检验统计量

根据总体分布、参数、已知/未知方差等选择 $Z$、$t$、$\\chi^2$ 或 $F$ 检验。

### 第三步：确定拒绝域

给定 $\\alpha$，查表得临界值，写出 $W$ 的表达式。

### 第四步：计算检验统计量

代入样本数据，计算 $T$ 的观测值 $t_{\\text{obs}}$。

### 第五步：做出决策

若 $t_{\\text{obs}} \\in W$，拒绝 $H_0$；否则不拒绝 $H_0$。可报告 $p$ 值。

---

## 例题 1：Z 检验完整流程

某厂声称产品重量均值 $\\mu = 500$ g，已知 $\\sigma = 10$ g。抽 $n = 16$ 件，$\\bar{x} = 504$ g。在 $\\alpha = 0.05$ 下检验。

**第一步**：$H_0: \\mu = 500$，$H_1: \\mu \\neq 500$（双侧）

**第二步**：$Z = \\frac{\\bar{X} - 500}{\\sigma/\\sqrt{n}} \\sim N(0,1)$

**第三步**：$W = \\{ |Z| \\ge 1.96 \\}$

**第四步**：$Z_{\\text{obs}} = \\frac{504 - 500}{10/4} = 1.6$

**第五步**：$|1.6| < 1.96$，不拒绝 $H_0$。

---

## 例题 2：t 检验完整流程

检验 $H_0: \\mu = 50$ vs $H_1: \\mu > 50$，$n = 10$，$\\bar{x} = 53$，$s = 4$，$\\alpha = 0.05$。

**第二步**：$t = \\frac{\\bar{X} - 50}{S/\\sqrt{n}} \\sim t(9)$

**第三步**：$W = \\{ t \\ge t_{0.05}(9) = 1.833 \\}$

**第四步**：$t_{\\text{obs}} = \\frac{53 - 50}{4/\\sqrt{10}} = 2.37$

**第五步**：$2.37 > 1.833$，拒绝 $H_0$。

---

## 例题 3：卡方检验拒绝域

$H_0: \\sigma^2 = 100$ vs $H_1: \\sigma^2 \\neq 100$，$n = 20$，$s^2 = 130$，$\\alpha = 0.05$。

$$ \\chi^2 = \\frac{(n-1)S^2}{\\sigma_0^2} = \\frac{19 \\times 130}{100} = 24.7 $$

双侧拒绝域：$\\chi^2 \\le \\chi^2_{0.975}(19) = 8.907$ 或 $\\chi^2 \\ge \\chi^2_{0.025}(19) = 32.852$。

$8.907 < 24.7 < 32.852$，不拒绝 $H_0$。

---

## 检验方法选择速查表

| 待检验参数 | $\\sigma^2$ 已知 | $\\sigma^2$ 未知 |
| :--- | :--- | :--- |
| 单总体均值 $\\mu$ | Z 检验 | t 检验 |
| 两总体均值差 | 双样本 Z | 双样本 t / Welch |
| 单总体方差 $\\sigma^2$ | — | $\\chi^2$ 检验 |
| 两方差比 | — | F 检验 |

---

## 常见误区

**误区 1**：接受域 = "$H_0$ 为真"。
> 正确：接受域是"不拒绝 $H_0$"的区域，包含 $H_0$ 为真和 $H_1$ 为真但证据不足两种情况。

**误区 2**：临界值与分位数混淆。
> 正确：$z_{\\alpha/2}$ 满足 $P(Z > z_{\\alpha/2}) = \\alpha/2$。

**误区 3**：跳过建立假设直接算统计量。
> 正确：$H_1$ 的方向决定单侧/双侧，进而决定拒绝域形式。

---

## 提示

- 五步法中，**第三步**（确定拒绝域）必须与 **第一步**（$H_0$, $H_1$）一致。
- 可同时用 p 值验证：$p \\le \\alpha \\iff$ 统计量落入拒绝域。
- 报告结果时写出：假设、统计量、临界值、p 值、结论。

<ManimAnimation src="/videos/ch08/rejection_region.mp4" title="拒绝域的构建过程动态演示" />

<InteractiveDistribution type="normal" mean={0} stdDev={1} />
`
    },
    {
      id: "8.6",
      title: "p值与统计显著性",
      content: `
## p 值的定义

**p 值 (p-value)**：在原假设 $H_0$ 为真的条件下，检验统计量取观测值或更极端值的概率。

形式化：设 $T$ 为检验统计量，$t_{\\text{obs}}$ 为观测值。

- **双侧**：$p = 2 \\min\\{ P(T \\le t_{\\text{obs}} \\mid H_0), P(T \\ge t_{\\text{obs}} \\mid H_0) \\}$
- **右侧**：$p = P(T \\ge t_{\\text{obs}} \\mid H_0)$
- **左侧**：$p = P(T \\le t_{\\text{obs}} \\mid H_0)$

---

## p 值的直观理解

p 值回答：**若 $H_0$ 为真，出现当前或更极端结果的可能性有多大？**

- $p$ 很小 → 在 $H_0$ 下该结果罕见 → 有理由怀疑 $H_0$
- $p$ 较大 → 在 $H_0$ 下该结果常见 → 无充分理由拒绝 $H_0$

**决策规则**：若 $p \\le \\alpha$，拒绝 $H_0$；否则不拒绝。

---

## p 值与拒绝域的关系

$p \\le \\alpha \\iff$ 检验统计量落入拒绝域 $W$。

p 值是"最小的 $\\alpha$"使得该观测值刚好落在拒绝域内。因此 p 值提供了比固定 $\\alpha$ 更细粒度的证据强度信息。

| p 值范围 | 通常解释 |
| :--- | :--- |
| $p < 0.001$ | 极强证据反对 $H_0$ |
| $0.001 \\le p < 0.01$ | 强证据 |
| $0.01 \\le p < 0.05$ | 中等证据 |
| $0.05 \\le p < 0.10$ | 弱证据 |
| $p \\ge 0.10$ | 几乎无证据 |

---

## 例题 1：Z 检验 p 值

$H_0: \\mu = 10$ vs $H_1: \\mu \\neq 10$，$Z_{\\text{obs}} = 2.3$。

$$ p = 2 P(Z \\ge 2.3) = 2 \\times 0.0107 = 0.0214 $$

$p = 0.0214 < 0.05$，在 $\\alpha = 0.05$ 下拒绝 $H_0$。

---

## 例题 2：t 检验 p 值

$n = 15$，$t_{\\text{obs}} = -2.1$，$H_1: \\mu < \\mu_0$（左侧）。

$$ p = P(t(14) \\le -2.1) \\approx 0.027 $$

---

## 例题 3：单侧 Z 检验

$H_0: p \\le 0.5$ vs $H_1: p > 0.5$，$n = 100$，$\\hat{p} = 0.58$。

$$ Z = \\frac{0.58 - 0.5}{\\sqrt{0.5 \\times 0.5 / 100}} = 1.6 $$

$$ p = P(Z \\ge 1.6) = 0.0548 $$

$\\alpha = 0.05$ 时不拒绝；$\\alpha = 0.10$ 时拒绝。

---

## 统计显著 vs 实际显著

**统计显著 (Statistical Significance)**：$p \\le \\alpha$，拒绝 $H_0$。

**实际显著 (Practical Significance)**：效应大小具有实际意义（如临床意义、经济意义）。

大样本下，微小差异也可能 $p$ 很小；小样本下，较大差异可能 $p$ 较大。应同时报告**效应量**（如 Cohen's d、均值差、相对风险等）。

---

## p 值争议与现代统计思维

- **ASA 声明 (2016)**：p 值不能衡量 $H_0$ 为真的概率，不能单独决定科学结论。
- **建议**：报告 p 值、效应量、置信区间；避免 p-hacking、选择性报告。
- **贝叶斯方法**：用后验概率补充频率派 p 值。

---

## 常见误解读

| 错误说法 | 正确理解 |
| :--- | :--- |
| p = 0.03 表示 $H_0$ 有 3% 概率为真 | p 是在 $H_0$ 下观测到该数据的概率 |
| p = 0.06 表示 $H_0$ 为真 | 只是证据不足以在 $\\alpha=0.05$ 下拒绝 |
| p 越小效应越大 | p 与效应量、样本量都有关 |
| 不显著 = 无效应 | 可能是功效不足（$\\beta$ 大） |

---

## p 值计算详解：不同检验类型

### Z 检验（双侧）

$$ p = 2 P(Z \\ge |z_{\\text{obs}}|) = 2\\left[1 - \\Phi(|z_{\\text{obs}}|)\\right] $$

### t 检验（双侧）

$$ p = 2 P(t(n-1) \\ge |t_{\\text{obs}}|) $$

需查 t 分布或软件计算。

### 单侧 p 值

- 右侧 $H_1: \\theta > \\theta_0$：$p = P(T \\ge t_{\\text{obs}} \\mid H_0)$
- 左侧 $H_1: \\theta < \\theta_0$：$p = P(T \\le t_{\\text{obs}} \\mid H_0)$

---

## 例题 4：手动计算 p 值

$n = 25$，$\\bar{x} = 102$，$\\sigma = 10$，检验 $H_0: \\mu = 100$ vs $H_1: \\mu \\neq 100$。

$$ z_{\\text{obs}} = \\frac{102-100}{10/5} = 1.0 $$

$$ p = 2 P(Z \\ge 1) = 2 \\times 0.1587 = 0.3174 $$

$p > 0.05$，不拒绝 $H_0$。

---

## 例题 5：p 值与 $\\alpha$ 的边界

若 $p = 0.048$，$\\alpha = 0.05$ → 拒绝；$\\alpha = 0.01$ → 不拒绝。

说明结论对 $\\alpha$ 选择敏感，应报告精确 p 值而非仅"显著/不显著"。

---

## 置信区间与 p 值的一致性

对 $\\mu$，95% 置信区间 $[\\bar{x} - 1.96\\sigma/\\sqrt{n}, \\bar{x} + 1.96\\sigma/\\sqrt{n}]$。

若 $\\mu_0$ 在区间外，则 $p < 0.05$（双侧）。两者提供互补信息：区间给**范围**，p 值给**证据强度**。

<FormulaSteps formula="p = P(|T| \\ge |t_{\\text{obs}}| \\mid H_0)" steps={[
  "在 $H_0$ 下确定 $T$ 的分布。",
  "计算 $t_{\\text{obs}} = T(\\text{样本})$。",
  "双侧：$p = 2 P(T \\ge |t_{\\text{obs}}| \\mid H_0)$。",
  "单侧：$p = P(T \\ge t_{\\text{obs}} \\mid H_0)$ 或 $P(T \\le t_{\\text{obs}} \\mid H_0)$。",
  "比较 $p$ 与 $\\alpha$ 做决策。"
]} />

<ManimAnimation src="/videos/ch08/p_value.mp4" title="p值的几何意义与计算过程动画" />

<InteractiveDistribution type="normal" mean={0} stdDev={1} />
`
    },
    {
      id: "8.7",
      title: "正态总体均值的Z检验",
      content: `
## 适用条件

**Z 检验 (Z-test)** 用于正态总体均值 $\\mu$ 的假设检验，必须同时满足：

1. 总体 $X \\sim N(\\mu, \\sigma^2)$（或 $n$ 足够大时可渐近使用）
2. **总体方差 $\\sigma^2$ 已知**（这是 Z 检验与 t 检验的根本区别）
3. $X_1, X_2, \\dots, X_n$ 为来自总体的**简单随机样本**

设 $\\bar{X} = \\frac{1}{n}\\sum X_i$ 为样本均值。若 $\\sigma^2$ 未知，**必须**改用 t 检验（见 8.8 节）。

> **注意**：有些题目中 $\\sigma$ 由长期历史数据或大量前期观测给出，可视为"已知"；若 $\\sigma$ 由当前样本估计，则应用 t 检验。

---

## 标准误与抽样分布

样本均值的标准差（标准误）为：
$$ SE(\\bar{X}) = \\frac{\\sigma}{\\sqrt{n}} $$

在 $H_0: \\mu = \\mu_0$ 成立时：
$$ \\bar{X} \\sim N\\left(\\mu_0, \\frac{\\sigma^2}{n}\\right) $$

样本容量 $n$ 越大，$\\bar{X}$ 的波动越小，检验越灵敏。

---

## 检验统计量的推导

<FormulaSteps formula="Z = \\frac{\\bar{X} - \\mu_0}{\\sigma / \\sqrt{n}} \\sim N(0, 1)" steps={[
  "在 $H_0: \\mu = \\mu_0$ 下，$E(\\bar{X}) = \\mu_0$，$D(\\bar{X}) = \\sigma^2/n$。",
  "由正态总体性质，$\\bar{X} \\sim N(\\mu_0, \\sigma^2/n)$。",
  "标准化变换：$Z = \\frac{\\bar{X} - \\mu_0}{\\sigma/\\sqrt{n}} = \\frac{\\bar{X} - \\mu_0}{SE(\\bar{X})}$。",
  "标准化后的 $Z \\sim N(0, 1)$，与未知参数无关（在 $H_0$ 下）。"
]} />

---

## 双侧 Z 检验

**假设**：
$$ H_0: \\mu = \\mu_0, \\quad H_1: \\mu \\neq \\mu_0 $$

**拒绝域**（显著性水平 $\\alpha$）：
$$ W = \\left\\{ |Z| \\ge z_{\\alpha/2} \\right\\} $$

**例题 1**：某元件平均寿命标准 $\\mu_0 = 1000$ 小时，已知 $\\sigma = 150$ 小时。抽 $n = 36$ 件，$\\bar{x} = 1050$ 小时，$\\alpha = 0.05$。

$$ Z = \\frac{1050 - 1000}{150/6} = \\frac{50}{25} = 2.0 $$

$z_{0.025} = 1.96$，$|2.0| > 1.96$，**拒绝** $H_0$。$p = 2 P(Z \\ge 2) = 0.0456$。

**例题 2**：$\\mu_0 = 100$，$\\sigma = 15$，$n = 36$，$\\bar{x} = 105$，$\\alpha = 0.05$。

$$ Z = \\frac{105 - 100}{15/6} = 2.0 $$

$|2.0| > 1.96$，**拒绝** $H_0$（与例题 1 类似结构）。

---

## 右侧 Z 检验

**假设**：
$$ H_0: \\mu \\le \\mu_0, \\quad H_1: \\mu > \\mu_0 $$

**拒绝域**：
$$ W = \\left\\{ Z \\ge z_\\alpha \\right\\} $$

**例题 3**：检验新型电池平均寿命是否**超过** 500 小时。$\\sigma = 40$，$n = 25$，$\\bar{x} = 520$，$\\alpha = 0.05$。

$$ Z = \\frac{520 - 500}{40/5} = \\frac{20}{8} = 2.5 $$

$z_{0.05} = 1.645$，$2.5 > 1.645$，**拒绝** $H_0$，有充分证据表明平均寿命超过 500 小时。

---

## 左侧 Z 检验

**假设**：
$$ H_0: \\mu \\ge \\mu_0, \\quad H_1: \\mu < \\mu_0 $$

**拒绝域**：
$$ W = \\left\\{ Z \\le -z_\\alpha \\right\\} $$

**例题 4**：环保标准规定某污染物平均浓度应**低于** 10 ppm。$\\sigma = 2$，$n = 16$，$\\bar{x} = 9.2$ ppm，$\\alpha = 0.05$。

$$ Z = \\frac{9.2 - 10}{2/4} = \\frac{-0.8}{0.5} = -1.6 $$

$-1.6 > -1.645$，在 $\\alpha = 0.05$ 下**不拒绝** $H_0$；$p = P(Z \\le -1.6) = 0.0548$，边界情况。

---

## Z 检验汇总表（考研必背）

| 检验类型 | $H_0$ | $H_1$ | 检验统计量 | 拒绝域 |
| :--- | :--- | :--- | :--- | :--- |
| 双侧 | $\\mu = \\mu_0$ | $\\mu \\neq \\mu_0$ | $Z = \\frac{\\bar{X}-\\mu_0}{\\sigma/\\sqrt{n}}$ | $\\|Z\\| \\ge z_{\\alpha/2}$ |
| 右侧 | $\\mu \\le \\mu_0$ | $\\mu > \\mu_0$ | 同上 | $Z \\ge z_\\alpha$ |
| 左侧 | $\\mu \\ge \\mu_0$ | $\\mu < \\mu_0$ | 同上 | $Z \\le -z_\\alpha$ |

---

## 例题 5：完整五步 Z 检验

某机器设定温度为 200°C，长期观测知 $\\sigma = 5$°C。现抽 20 次，$\\bar{x} = 203$°C。在 $\\alpha = 0.01$ 下检验温度是否偏离设定值。

**第一步**：$H_0: \\mu = 200$，$H_1: \\mu \\neq 200$（双侧）

**第二步**：$Z = \\frac{\\bar{X} - 200}{5/\\sqrt{20}} \\sim N(0,1)$

**第三步**：$\\alpha = 0.01$，$W = \\{ |Z| \\ge z_{0.005} = 2.576 \\}$

**第四步**：$Z_{\\text{obs}} = \\frac{203 - 200}{5/\\sqrt{20}} = \\frac{3}{1.118} = 2.68$

**第五步**：$|2.68| > 2.576$，**拒绝** $H_0$。$p \\approx 0.0074 < 0.01$。

---

## 例题 6：比例问题的 Z 近似

当 $n$ 较大时，比例 $\\hat{p}$ 的检验也可用 Z 统计量（见 8.1 节）。设 $H_0: p = p_0$，则：

$$ Z = \\frac{\\hat{p} - p_0}{\\sqrt{p_0(1-p_0)/n}} \\approx N(0, 1) $$

例如 $n = 200$，$\\hat{p} = 0.06$，检验 $H_0: p \\le 0.03$ vs $H_1: p > 0.03$：

$$ Z = \\frac{0.06 - 0.03}{\\sqrt{0.03 \\times 0.97 / 200}} \\approx 2.47 > 1.645 $$

拒绝 $H_0$。

---

## 与置信区间的对偶关系

$\\mu$ 的 $1-\\alpha$ 置信区间为：
$$ \\left[ \\bar{X} - z_{\\alpha/2} \\frac{\\sigma}{\\sqrt{n}}, \\; \\bar{X} + z_{\\alpha/2} \\frac{\\sigma}{\\sqrt{n}} \\right] $$

**对偶原理**：在 $\\alpha$ 水平下拒绝 $H_0: \\mu = \\mu_0$ $\\iff$ $\\mu_0$ 不在上述置信区间内。

---

## 常见误区

**误区 1**：$\\sigma^2$ 未知仍用 Z 检验，用 $S$ 代替 $\\sigma$ 但不改分布。
> 正确：$\\sigma$ 未知时必须用 t 检验，统计量服从 t 分布。

**误区 2**：双侧检验用 $z_\\alpha$ 而非 $z_{\\alpha/2}$。
> 正确：双侧时总 $\\alpha$ 分摊到两尾，各 $\\alpha/2$。

**误区 3**：混淆 $H_0: \\mu = \\mu_0$ 与 $H_0: \\mu \\le \\mu_0$ 的拒绝域。
> 正确：等号假设用双侧；复合假设 $H_0: \\mu \\le \\mu_0$ 用右侧检验时，拒绝域在右尾。

---

## 提示

- 查表时注意：$z_{\\alpha/2}$ 是**上侧** $\\alpha/2$ 分位数，$P(Z > z_{\\alpha/2}) = \\alpha/2$。
- 常用值：$z_{0.025} = 1.96$，$z_{0.05} = 1.645$，$z_{0.005} = 2.576$，$z_{0.01} = 2.326$。
- 做题时先判断 $\\sigma$ 是否已知，再选 Z 或 t。

<ManimAnimation src="/videos/ch08/z_test.mp4" title="Z检验的完整流程动态演示" />

<InteractiveDistribution type="normal" mean={0} stdDev={1} />
`
    },
    {
      id: "8.8",
      title: "正态总体均值的t检验",
      content: `
## 适用条件

**t 检验 (t-test)** 用于正态总体均值 $\\mu$ 的假设检验，当总体方差 $\\sigma^2$ **未知**时，必须用样本标准差 $S$ 来估计 $\\sigma$。

设 $X_1, X_2, \\dots, X_n$ 是来自总体 $X \\sim N(\\mu, \\sigma^2)$ 的简单随机样本，$\\bar{X}$ 为样本均值，$S^2 = \\frac{1}{n-1}\\sum_{i=1}^n (X_i - \\bar{X})^2$ 为样本方差。

> **注意**：若 $\\sigma^2$ 已知，应使用 Z 检验（见 8.7 节）；若总体非正态但 $n$ 很大，可借助中心极限定理近似使用 t 检验，但严格来说需要正态总体假设。

---

## 为何用 t 分布而非标准正态？

当 $\\sigma$ 未知时，若仍用 $Z = \\frac{\\bar{X} - \\mu_0}{\\sigma/\\sqrt{n}}$，统计量无法计算（$\\sigma$ 未知）。自然想到用 $S$ 代替 $\\sigma$：

$$ T^* = \\frac{\\bar{X} - \\mu_0}{S / \\sqrt{n}} $$

但 $S$ 本身是随机变量，用 $S$ 代替 $\\sigma$ 引入了**额外的估计误差**。可以严格证明 $T^*$ 不再服从标准正态分布，而服从**自由度为 $n-1$ 的 t 分布**。

<FormulaSteps formula="t = \\frac{\\bar{X} - \\mu_0}{S / \\sqrt{n}} \\sim t(n-1)" steps={[
  "在 $H_0: \\mu = \\mu_0$ 下，$U = \\frac{\\bar{X} - \\mu_0}{\\sigma/\\sqrt{n}} \\sim N(0, 1)$。",
  "根据正态总体抽样分布定理，$V = \\frac{(n-1)S^2}{\\sigma^2} \\sim \\chi^2(n-1)$，且 $U$ 与 $V$ 相互独立。",
  "由 t 分布的定义：若 $U \\sim N(0,1)$，$V \\sim \\chi^2(df)$ 独立，则 $t = \\frac{U}{\\sqrt{V/df}} \\sim t(df)$。",
  "代入 $U$ 和 $V$，令 $df = n-1$，化简得 $t = \\frac{\\bar{X} - \\mu_0}{S/\\sqrt{n}} \\sim t(n-1)$。",
  "t 分布尾部比标准正态更厚（厚尾），小样本时比 Z 检验更保守。"
]} />

---

## Z 检验 vs t 检验对比

| 对比项 | Z 检验 | t 检验 |
| :--- | :--- | :--- |
| 方差 | $\\sigma^2$ 已知 | $\\sigma^2$ 未知 |
| 统计量 | $Z = \\frac{\\bar{X}-\\mu_0}{\\sigma/\\sqrt{n}}$ | $t = \\frac{\\bar{X}-\\mu_0}{S/\\sqrt{n}}$ |
| 分布 | $N(0, 1)$ | $t(n-1)$ |
| 临界值 | $z_{\\alpha/2}$ | $t_{\\alpha/2}(n-1)$ |
| 大样本 | 精确 | $t(n-1) \\approx N(0,1)$ |

> **提示**：当 $n > 30$ 时，$t$ 分布与标准正态非常接近，有些教材允许用 Z 近似；但考研中 $\\sigma^2$ 未知时**必须**用 t 检验。

---

## 双侧 t 检验

**假设**：$H_0: \\mu = \\mu_0$，$H_1: \\mu \\neq \\mu_0$

**拒绝域**（显著性水平 $\\alpha$）：
$$ W = \\left\\{ |t| \\ge t_{\\alpha/2}(n-1) \\right\\} $$

**例题 1（完整五步）**：某元件额定寿命 $\\mu_0 = 500$ 小时。抽 $n = 12$ 件，$\\bar{x} = 520$，$s = 30$，$\\alpha = 0.05$。

1. $H_0: \\mu = 500$，$H_1: \\mu \\neq 500$
2. $t = \\frac{\\bar{X} - 500}{S/\\sqrt{12}} \\sim t(11)$
3. $W = \\{ |t| \\ge t_{0.025}(11) = 2.201 \\}$
4. $t_{\\text{obs}} = \\frac{520 - 500}{30/\\sqrt{12}} = \\frac{20}{8.66} = 2.31$
5. $|2.31| > 2.201$，**拒绝** $H_0$。$p = 2 P(t(11) \\ge 2.31) \\approx 0.041$

---

## 单侧 t 检验

**右侧检验**：$H_0: \\mu \\le \\mu_0$，$H_1: \\mu > \\mu_0$，拒绝域 $t \\ge t_\\alpha(n-1)$

**左侧检验**：$H_0: \\mu \\ge \\mu_0$，$H_1: \\mu < \\mu_0$，拒绝域 $t \\le -t_\\alpha(n-1)$

**例题 2**：检验新药是否**提高**某生理指标。$n = 8$，$\\bar{x} = 85$，$s = 6$，$\\mu_0 = 80$，$\\alpha = 0.05$。

$$ t = \\frac{85 - 80}{6/\\sqrt{8}} = \\frac{5}{2.121} = 2.36 $$

$t_{0.05}(7) = 1.895$，$2.36 > 1.895$，**拒绝** $H_0$。

**例题 3（左侧）**：检验污染物浓度是否**低于**标准 10 ppm。$n = 16$，$\\bar{x} = 9.2$，$s = 2.4$。

$$ t = \\frac{9.2 - 10}{2.4/4} = -1.33 $$

$t_{0.05}(15) = 1.753$，$-1.33 > -1.753$，不拒绝 $H_0$；$p = P(t(15) \\le -1.33) \\approx 0.103$。

---

## 样本量对 t 检验的影响

当 $n \\to \\infty$ 时：
- $S \\xrightarrow{P} \\sigma$
- $t(n-1) \\to N(0, 1)$

| 自由度 $n-1$ | $t_{0.025}(n-1)$ | $z_{0.025}$ |
| :---: | :---: | :---: |
| 5 | 2.571 | 1.960 |
| 10 | 2.228 | 1.960 |
| 20 | 2.086 | 1.960 |
| 30 | 2.042 | 1.960 |
| $\\infty$ | 1.960 | 1.960 |

小样本时 t 临界值明显大于 Z 临界值，体现了对方差估计不确定性的补偿。

---

## 例题 4：完整 t 检验（考研典型题）

设 $X \\sim N(\\mu, \\sigma^2)$，$\\sigma^2$ 未知。$n = 20$，$\\bar{x} = 96$，$s = 8$。在 $\\alpha = 0.05$ 下检验 $H_0: \\mu = 100$ vs $H_1: \\mu \\neq 100$。

$$ t = \\frac{96 - 100}{8/\\sqrt{20}} = \\frac{-4}{1.789} = -2.24 $$

$t_{0.025}(19) = 2.093$，$|-2.24| > 2.093$，**拒绝** $H_0$。

---

## 常见误区

**误区 1**：$\\sigma^2$ 未知时仍用 Z 检验。
> 正确：必须用 t 检验，用 $S$ 代替 $\\sigma$。

**误区 2**：混淆 $t_{\\alpha/2}(n-1)$ 与 $t_\\alpha(n-1)$。
> 正确：双侧用 $t_{\\alpha/2}$，单侧用 $t_\\alpha$。

**误区 3**：自由度用 $n$ 而非 $n-1$。
> 正确：单样本 t 检验自由度恒为 $df = n - 1$。

---

## t 检验汇总表

| 检验类型 | $H_0$ | $H_1$ | 拒绝域 |
| :--- | :--- | :--- | :--- |
| 双侧 | $\\mu = \\mu_0$ | $\\mu \\neq \\mu_0$ | $\\|t\\| \\ge t_{\\alpha/2}(n-1)$ |
| 右侧 | $\\mu \\le \\mu_0$ | $\\mu > \\mu_0$ | $t \\ge t_\\alpha(n-1)$ |
| 左侧 | $\\mu \\ge \\mu_0$ | $\\mu < \\mu_0$ | $t \\le -t_\\alpha(n-1)$ |

<ManimAnimation src="/videos/ch08/t_test.mp4" title="t检验与Z检验的对比动画" />

<InteractiveDistribution type="normal" mean={0} stdDev={1} />
`
    },
    {
      id: "8.9",
      title: "两个正态总体均值的比较",
      content: `
## 问题背景与设定

在实际应用中，我们常需比较两个总体的均值是否相等，例如：
- 两种工艺生产的产品质量是否有差异
- 两种药物疗效是否不同
- 男女考生成绩均值是否相等

设 $X_1, \\dots, X_{n_1}$ 来自总体 $X \\sim N(\\mu_1, \\sigma_1^2)$，$\\bar{X}, S_1^2$ 为样本均值和方差；
$Y_1, \\dots, Y_{n_2}$ 来自总体 $Y \\sim N(\\mu_2, \\sigma_2^2)$，$\\bar{Y}, S_2^2$ 为样本均值和方差。两样本**相互独立**。

一般检验：
$$ H_0: \\mu_1 - \\mu_2 = \\delta, \\quad H_1: \\mu_1 - \\mu_2 \\neq \\delta $$
其中 $\\delta$ 为已知常数，**最常取 $\\delta = 0$**（即检验两均值是否相等）。

---

## 方法一：双样本 Z 检验（两总体方差已知）

当 $\\sigma_1^2, \\sigma_2^2$ **已知**时，利用两独立正态样本均值之差的分布：

$$ \\bar{X} - \\bar{Y} \\sim N\\left(\\mu_1 - \\mu_2, \\; \\frac{\\sigma_1^2}{n_1} + \\frac{\\sigma_2^2}{n_2}\\right) $$

在 $H_0: \\mu_1 - \\mu_2 = \\delta$ 下，构造标准正态统计量：

$$ Z = \\frac{(\\bar{X} - \\bar{Y}) - \\delta}{\\sqrt{\\frac{\\sigma_1^2}{n_1} + \\frac{\\sigma_2^2}{n_2}}} \\sim N(0, 1) $$

**拒绝域**（双侧）：$|Z| \\ge z_{\\alpha/2}$

**例题 1**：比较两班数学成绩。$n_1 = 30$，$n_2 = 25$，$\\bar{x} = 78$，$\\bar{y} = 72$，已知 $\\sigma_1 = \\sigma_2 = 10$，$\\alpha = 0.05$。

$$ Z = \\frac{78 - 72}{\\sqrt{100/30 + 100/25}} = \\frac{6}{2.68} = 2.24 $$

$|2.24| > 1.96$，**拒绝** $H_0: \\mu_1 = \\mu_2$，两班均值有显著差异。

---

## 方法二：双样本 t 检验（方差未知但相等）

当 $\\sigma_1^2 = \\sigma_2^2 = \\sigma^2$ **未知**时，需要联合估计 $\\sigma^2$，定义**合并样本方差 (Pooled Variance)**：

$$ S_p^2 = \\frac{(n_1 - 1)S_1^2 + (n_2 - 1)S_2^2}{n_1 + n_2 - 2} $$

<FormulaSteps formula="t = \\frac{(\\bar{X} - \\bar{Y}) - \\delta}{S_p \\sqrt{\\frac{1}{n_1} + \\frac{1}{n_2}}} \\sim t(n_1 + n_2 - 2)" steps={[
  "两独立正态样本：$\\bar{X} - \\bar{Y} \\sim N(\\mu_1 - \\mu_2, \\sigma^2(\\frac{1}{n_1} + \\frac{1}{n_2}))$。",
  "标准化：$U = \\frac{(\\bar{X}-\\bar{Y})-(\\mu_1-\\mu_2)}{\\sigma\\sqrt{1/n_1 + 1/n_2}} \\sim N(0,1)$。",
  "两样本方差：$\\frac{(n_1-1)S_1^2}{\\sigma^2} \\sim \\chi^2(n_1-1)$，$\\frac{(n_2-1)S_2^2}{\\sigma^2} \\sim \\chi^2(n_2-1)$，独立。",
  "相加：$V = \\frac{(n_1-1)S_1^2 + (n_2-1)S_2^2}{\\sigma^2} \\sim \\chi^2(n_1+n_2-2)$。",
  "构造 t 统计量 $t = U/\\sqrt{V/(n_1+n_2-2)}$，代入化简，定义 $S_p = \\sqrt{S_p^2}$，得 $t \\sim t(n_1+n_2-2)$。"
]} />

**拒绝域**（双侧）：$|t| \\ge t_{\\alpha/2}(n_1 + n_2 - 2)$

**例题 2**：两种合成材料强度比较。$n_1 = 12$，$n_2 = 10$，$\\bar{x} = 85$，$\\bar{y} = 78$，$s_1^2 = 64$，$s_2^2 = 49$，$\\alpha = 0.05$。

$$ S_p^2 = \\frac{11 \\times 64 + 9 \\times 49}{20} = 57.25, \\quad S_p = 7.57 $$

$$ t = \\frac{85 - 78}{7.57 \\sqrt{1/12 + 1/10}} = \\frac{7}{3.24} = 2.16 $$

$t_{0.025}(20) = 2.086$，$|2.16| > 2.086$，**拒绝** $H_0$。

---

## 方法三：Welch t 检验（方差未知且不等）

当 $\\sigma_1^2 \\neq \\sigma_2^2$ 时，合并方差 $S_p^2$ 的假设不成立，应使用 **Welch t 检验**：

$$ t_W = \\frac{(\\bar{X} - \\bar{Y}) - \\delta}{\\sqrt{\\frac{S_1^2}{n_1} + \\frac{S_2^2}{n_2}}} $$

自由度由 Welch-Satterthwaite 公式近似：
$$ df = \\frac{\\left(\\frac{S_1^2}{n_1} + \\frac{S_2^2}{n_2}\\right)^2}{\\frac{(S_1^2/n_1)^2}{n_1-1} + \\frac{(S_2^2/n_2)^2}{n_2-1}} $$

> **提示**：考研中 Welch 检验常作了解；做题前先判断方差是否相等（可用 F 检验，见 8.12 节）。

---

## 例题 3：完整两样本 t 检验

比较工艺 A 与 B 的产品尺寸。$H_0: \\mu_A = \\mu_B$，$n_A = 15$，$n_B = 12$，$\\bar{x}_A = 102$，$\\bar{x}_B = 95$，$s_A^2 = 25$，$s_B^2 = 36$。

$$ S_p^2 = \\frac{14 \\times 25 + 11 \\times 36}{25} = 29.84 $$

$$ t = \\frac{102 - 95}{\\sqrt{29.84} \\sqrt{1/15 + 1/12}} = \\frac{7}{2.24} = 3.12 $$

$t_{0.025}(25) = 2.060$，$|3.12| > 2.060$，**拒绝** $H_0$。

---

## 方法选择流程图

| 条件 | 推荐方法 |
| :--- | :--- |
| $\\sigma_1^2, \\sigma_2^2$ 已知 | 双样本 Z 检验 |
| $\\sigma_1^2 = \\sigma_2^2$ 未知 | 合并方差 t 检验 |
| $\\sigma_1^2 \\neq \\sigma_2^2$ 未知 | Welch t 检验 |

---

## 常见误区

**误区 1**：未检验方差相等就直接用合并 t 检验。
> 正确：若方差差异大，应先用 F 检验判断，或直接用 Welch 检验。

**误区 2**：混淆单样本与两样本问题。
> 正确：两独立总体用两样本检验；配对数据用配对 t 检验（8.10 节）。

**误区 3**：自由度用 $n_1 + n_2$ 而非 $n_1 + n_2 - 2$。
> 正确：合并 t 检验自由度 $df = n_1 + n_2 - 2$。

<ManimAnimation src="/videos/ch08/two_sample.mp4" title="两样本均值检验的统计量构造动画" />

<InteractiveDistribution type="normal" mean={0} stdDev={1} />
`
    },
    {
      id: "8.10",
      title: "配对样本t检验",
      content: `
## 配对设计的本质

**配对样本 (Paired Samples)** 是指两个样本的观测值之间存在一一对应关系，因此**不独立**。若误用独立两样本 t 检验，会忽略配对带来的信息，降低检验功效。

**典型配对场景**：
- **前后测量**：同一患者用药前后的血压、体重
- **匹配对照**：双胞胎、同窝动物分别接受两种处理
- **重复测量**：同一台仪器在两种条件下的读数
- **左右对照**：同一受试者左眼/右眼、左手/右手

> **关键判别**：若数据可以写成 $(X_i, Y_i)$ 对，且 $X_i$ 与 $Y_i$ 相关，则用配对 t 检验；若两组被试完全独立随机分配，则用两样本 t 检验。

---

## 核心思想：差值化

对每一对观测计算差值：
$$ D_i = X_i - Y_i, \\quad i = 1, 2, \\dots, n $$

假定差值 $D_i$ 来自正态总体 $D \\sim N(\\mu_d, \\sigma_d^2)$。原问题转化为对 $\\mu_d = E(D)$ 的**单样本 t 检验**：

$$ H_0: \\mu_d = d_0 \\quad (\\text{通常 } d_0 = 0), \\quad H_1: \\mu_d \\neq d_0 \\text{ 或单侧} $$

计算差值样本的均值和标准差：
$$ \\bar{D} = \\frac{1}{n}\\sum_{i=1}^n D_i, \\quad S_D = \\sqrt{\\frac{1}{n-1}\\sum_{i=1}^n (D_i - \\bar{D})^2} $$

<FormulaSteps formula="t = \\frac{\\bar{D} - d_0}{S_D / \\sqrt{n}} \\sim t(n-1)" steps={[
  "对每对数据计算 $D_i = X_i - Y_i$。",
  "求 $\\bar{D}$ 和 $S_D$（差值样本的均值与标准差）。",
  "在 $H_0: \\mu_d = d_0$ 下，$\\bar{D} \\sim N(d_0, \\sigma_d^2/n)$。",
  "用 $S_D$ 估计 $\\sigma_d$，构造 $t = \\frac{\\bar{D} - d_0}{S_D/\\sqrt{n}}$。",
  "在 $H_0$ 下 $t \\sim t(n-1)$，拒绝域与单样本 t 检验完全相同。"
]} />

---

## 为何配对 t 检验功效更高？

配对设计通过差值 $D_i = X_i - Y_i$ **消除了个体间差异**（如不同患者的基线水平），使 $\\sigma_d^2$ 通常远小于 $\\sigma_X^2 + \\sigma_Y^2$，从而标准误 $S_D/\\sqrt{n}$ 更小，更容易检测出真实差异。

| 设计 | 有效方差 | 功效 |
| :--- | :--- | :--- |
| 独立两样本 | $\\sigma_1^2/n_1 + \\sigma_2^2/n_2$ | 较低 |
| 配对 | $\\sigma_d^2/n$ | 较高（若 $\\sigma_d^2$ 小） |

---

## 例题 1：减肥计划（左侧检验）

10 人参加 12 周减肥计划，记录前后体重（kg）。差值 $d_i = \\text{后} - \\text{前}$（负值表示减轻）：

$\\bar{d} = -2.5$，$s_d = 2.0$，$n = 10$。检验 $H_0: \\mu_d = 0$ vs $H_1: \\mu_d < 0$（计划有效即体重下降）。

$$ t = \\frac{-2.5 - 0}{2.0/\\sqrt{10}} = \\frac{-2.5}{0.632} = -3.95 $$

$t_{0.05}(9) = 1.833$，$t = -3.95 < -1.833$，**拒绝** $H_0$，计划显著降低体重。$p \\approx 0.0016$。

---

## 例题 2：两种键盘打字速度（双侧）

10 名打字员分别用键盘 A、B 各打一篇文稿，记录时间（分钟）。差值 $d_i = t_B - t_A$（正值表示 B 更慢）：

$\\bar{d} = 1.2$，$s_d = 2.5$。检验 $H_0: \\mu_d = 0$ vs $H_1: \\mu_d \\neq 0$。

$$ t = \\frac{1.2}{2.5/\\sqrt{10}} = 1.52 $$

$t_{0.025}(9) = 2.262$，$|1.52| < 2.262$，**不拒绝** $H_0$，无证据表明两种键盘速度有差异。

---

## 例题 3：员工培训效果

8 名员工接受培训，记录前后工作效率评分（0–100）。$\\bar{d} = 5$，$s_d = 4$，$\\alpha = 0.05$，双侧检验。

$$ t = \\frac{5}{4/\\sqrt{8}} = \\frac{5}{1.414} = 3.54 $$

$t_{0.025}(7) = 2.365$，$|3.54| > 2.365$，**拒绝** $H_0$，培训显著提高工作效率。

---

## 配对 vs 独立两样本：决策表

| 问题特征 | 正确方法 |
| :--- | :--- |
| 同一对象两次测量 | 配对 t 检验 |
| 匹配的分组（如双胞胎） | 配对 t 检验 |
| 两独立随机组 | 两样本 t 检验 |
| 样本量不等且无配对 | 两样本 t 检验 |

---

## 常见误区

**误区 1**：配对数据用独立两样本 t 检验。
> 正确：配对数据必须做差值后单样本 t 检验，否则可能严重低估功效或错误推断。

**误区 2**：差值方向搞反。
> 正确：$D_i = X_i - Y_i$ 的方向应与 $H_1$ 一致；若 $H_1: \\mu_A > \\mu_B$，则设 $D_i = A_i - B_i$。

**误区 3**：自由度用 $2n$ 或 $n_1 + n_2 - 2$。
> 正确：配对 t 检验自由度为 $df = n - 1$（$n$ 为配对数）。

---

## 提示

- 做配对 t 检验前，可先画差值的直方图或 Q-Q 图检查正态性。
- 若 $n$ 较大（如 $n > 30$），即使差值轻度偏离正态，t 检验仍较稳健。
- 报告结果时说明配对设计，并给出 $\\bar{d}$ 和 $s_d$。

<ManimAnimation src="/videos/ch08/paired_test.mp4" title="配对t检验的原理与计算动画" />

<InteractiveDistribution type="normal" mean={0} stdDev={1} />
`
    },
    {
      id: "8.11",
      title: "正态总体方差的χ²检验",
      content: `
## 为什么需要检验方差

均值反映**集中趋势**，方差反映**离散程度（波动性）**。在许多实际问题中，方差与均值同等重要：

- **工业质量控制**：产品尺寸/重量的波动是否在允许范围内
- **金融风险管理**：资产收益率的波动（风险）是否发生变化
- **实验可靠性**：测量仪器的精度（重复性）是否稳定
- **假设检验前提**：两样本 t 检验要求 $\\sigma_1^2 = \\sigma_2^2$，需先用 F 检验验证

---

## 检验问题的一般形式

设总体 $X \\sim N(\\mu, \\sigma^2)$，$X_1, \\dots, X_n$ 为简单随机样本，$S^2$ 为样本方差。

- **双侧**：$H_0: \\sigma^2 = \\sigma_0^2$ vs $H_1: \\sigma^2 \\neq \\sigma_0^2$
- **右侧**：$H_0: \\sigma^2 \\le \\sigma_0^2$ vs $H_1: \\sigma^2 > \\sigma_0^2$
- **左侧**：$H_0: \\sigma^2 \\ge \\sigma_0^2$ vs $H_1: \\sigma^2 < \\sigma_0^2$

---

## 检验统计量的推导（$\\mu$ 未知）

<FormulaSteps formula="\\chi^2 = \\frac{(n-1)S^2}{\\sigma_0^2} \\sim \\chi^2(n-1)" steps={[
  "在 $H_0: \\sigma^2 = \\sigma_0^2$ 下，总体方差为 $\\sigma_0^2$。",
  "由正态总体抽样分布定理：若 $\\mu$ 未知，$\\frac{(n-1)S^2}{\\sigma^2} \\sim \\chi^2(n-1)$。",
  "在 $H_0$ 成立时，用 $\\sigma_0^2$ 替换真实方差 $\\sigma^2$。",
  "得检验统计量 $\\chi^2 = \\frac{(n-1)S^2}{\\sigma_0^2}$，在 $H_0$ 下服从 $\\chi^2(n-1)$。"
]} />

---

## 卡方分布的非对称性

$\\chi^2$ 分布是**右偏**的，因此：
- 双侧拒绝域**不对称**：左尾和右尾临界值不相等
- 不能简单用 $\\pm$ 对称临界值

**双侧拒绝域**（$\\mu$ 未知，$\\alpha$ 显著性水平）：
$$ W = \\left\\{ \\chi^2 \\le \\chi^2_{1-\\alpha/2}(n-1) \\quad \\text{或} \\quad \\chi^2 \\ge \\chi^2_{\\alpha/2}(n-1) \\right\\} $$

**单侧拒绝域**：
- 右侧：$\\chi^2 \\ge \\chi^2_\\alpha(n-1)$
- 左侧：$\\chi^2 \\le \\chi^2_{1-\\alpha}(n-1)$

---

## 特殊情形：$\\mu$ 已知

若总体均值 $\\mu$ **已知**，用 $\\sum (X_i - \\mu)^2$ 代替 $(n-1)S^2$：

$$ \\chi^2 = \\sum_{i=1}^n \\frac{(X_i - \\mu)^2}{\\sigma_0^2} \\sim \\chi^2(n) $$

> **注意**：自由度为 $n$（不是 $n-1$），因为未用样本均值估计 $\\mu$，少损失一个自由度。

---

## 例题 1：产品质量波动（双侧）

某产品重量方差标准 $\\sigma_0^2 = 100$ g²。抽 $n = 20$ 件，$s^2 = 130$ g²，$\\alpha = 0.05$。

$$ \\chi^2 = \\frac{19 \\times 130}{100} = 24.7 $$

查表：$\\chi^2_{0.975}(19) = 8.907$，$\\chi^2_{0.025}(19) = 32.852$。

$8.907 < 24.7 < 32.852$，**不拒绝** $H_0$，无证据表明方差偏离标准。

---

## 例题 2：方差是否增大（右侧）

精密加工要求 $\\sigma \\le 4$ mm，即 $\\sigma^2 \\le 16$。抽 $n = 15$ 件，$s^2 = 28$ mm²，$\\alpha = 0.05$。

$H_0: \\sigma^2 \\le 16$ vs $H_1: \\sigma^2 > 16$

$$ \\chi^2 = \\frac{14 \\times 28}{16} = 24.5 $$

$\\chi^2_{0.05}(14) = 23.685$，$24.5 > 23.685$，**拒绝** $H_0$，加工波动显著超标。

---

## 例题 3：方差是否减小（左侧）

某改进工艺声称降低了产品重量波动。$H_0: \\sigma^2 \\ge 25$ vs $H_1: \\sigma^2 < 25$，$n = 10$，$s^2 = 12$。

$$ \\chi^2 = \\frac{9 \\times 12}{25} = 4.32 $$

$\\chi^2_{0.95}(9) = 3.325$，$4.32 > 3.325$，**不拒绝** $H_0$（注意：左侧拒绝域是 $\\chi^2 \\le 3.325$，4.32 不在拒绝域内）。

---

## $\\chi^2$ 检验汇总表

| 情形 | 统计量 | 自由度 | 双侧拒绝域 |
| :--- | :--- | :---: | :--- |
| $\\mu$ 未知 | $\\frac{(n-1)S^2}{\\sigma_0^2}$ | $n-1$ | $\\le \\chi^2_{1-\\alpha/2}$ 或 $\\ge \\chi^2_{\\alpha/2}$ |
| $\\mu$ 已知 | $\\frac{\\sum(X_i-\\mu)^2}{\\sigma_0^2}$ | $n$ | 同上（自由度换为 $n$） |

---

## 常见误区

**误区 1**：双侧拒绝域关于某点对称。
> 正确：$\\chi^2$ 右偏，左尾临界值 $\\chi^2_{1-\\alpha/2}$ 远小于右尾 $\\chi^2_{\\alpha/2}$。

**误区 2**：$\\mu$ 已知仍用 $(n-1)S^2$。
> 正确：$\\mu$ 已知时用 $\\sum(X_i-\\mu)^2$，自由度为 $n$。

**误区 3**：混淆标准差与方差的检验。
> 正确：检验的是 $\\sigma^2$，但 $H_0$ 也可写成 $\\sigma = \\sigma_0$，统计量仍用方差形式。

<ManimAnimation src="/videos/ch08/chi_square_test.mp4" title="χ²检验的分布特征与拒绝域动画" />

<InteractiveDistribution type="normal" mean={0} stdDev={1} />
`
    },
    {
      id: "8.12",
      title: "两个正态总体方差比较的F检验",
      content: `
## F 分布的定义与性质

设 $U \\sim \\chi^2(m)$，$V \\sim \\chi^2(n)$ 相互独立，则：

$$ F = \\frac{U/m}{V/n} \\sim F(m, n) $$

$F$ 分布有两个自由度参数 $m$ 和 $n$，也是**右偏**分布。用于比较两个正态总体的方差是否相等。

**重要性质**：
- 若 $F \\sim F(m, n)$，则 $\\frac{1}{F} \\sim F(n, m)$
- $F_{1-\\alpha}(m, n) = \\frac{1}{F_\\alpha(n, m)}$（倒数关系）

---

## 问题设定

设 $X \\sim N(\\mu_1, \\sigma_1^2)$，$Y \\sim N(\\mu_2, \\sigma_2^2)$ 独立。从两总体分别抽取容量 $n_1, n_2$ 的样本，样本方差为 $S_1^2, S_2^2$。

检验：
$$ H_0: \\sigma_1^2 = \\sigma_2^2 \\quad \\text{vs} \\quad H_1: \\sigma_1^2 \\neq \\sigma_2^2 \\text{ 或单侧} $$

---

## F 检验统计量的推导

<FormulaSteps formula="F = \\frac{S_1^2}{S_2^2} \\sim F(n_1-1, n_2-1)" steps={[
  "由抽样分布定理：$\\frac{(n_1-1)S_1^2}{\\sigma_1^2} \\sim \\chi^2(n_1-1)$，$\\frac{(n_2-1)S_2^2}{\\sigma_2^2} \\sim \\chi^2(n_2-1)$，且独立。",
  "由 F 分布定义：$F^* = \\frac{S_1^2/\\sigma_1^2}{S_2^2/\\sigma_2^2} \\sim F(n_1-1, n_2-1)$。",
  "在 $H_0: \\sigma_1^2 = \\sigma_2^2$ 下，分子分母中的 $\\sigma_1^2$ 和 $\\sigma_2^2$ 相消。",
  "检验统计量简化为 $F = \\frac{S_1^2}{S_2^2} \\sim F(n_1-1, n_2-1)$。"
]} />

---

## 双侧 F 检验的拒绝域

$$ W = \\left\\{ F \\ge F_{\\alpha/2}(n_1-1, n_2-1) \\quad \\text{或} \\quad F \\le F_{1-\\alpha/2}(n_1-1, n_2-1) \\right\\} $$

利用倒数关系：$F_{1-\\alpha/2}(n_1-1, n_2-1) = \\frac{1}{F_{\\alpha/2}(n_2-1, n_1-1)}$

---

## "大方差作分子"技巧

F 分布表通常只给出**上侧分位数**。为简化计算，约定：

设 $S_{\\max}^2 = \\max(S_1^2, S_2^2)$，$S_{\\min}^2 = \\min(S_1^2, S_2^2)$，对应样本量 $n_{\\max}, n_{\\min}$。

构造：
$$ F = \\frac{S_{\\max}^2}{S_{\\min}^2} \\sim F(n_{\\max}-1, n_{\\min}-1) $$

**双侧检验**只需判断：
$$ F \\ge F_{\\alpha/2}(n_{\\max}-1, n_{\\min}-1) $$

若成立，则拒绝 $H_0: \\sigma_1^2 = \\sigma_2^2$。

> **提示**：此技巧避免了查下侧分位数，考研计算中非常实用。

---

## 单侧 F 检验

- **右侧** ($H_0: \\sigma_1^2 \\le \\sigma_2^2$ vs $H_1: \\sigma_1^2 > \\sigma_2^2$)：
  $$ F = \\frac{S_1^2}{S_2^2} \\ge F_\\alpha(n_1-1, n_2-1) $$

- **左侧** ($H_0: \\sigma_1^2 \\ge \\sigma_2^2$ vs $H_1: \\sigma_1^2 < \\sigma_2^2$)：
  $$ F' = \\frac{S_2^2}{S_1^2} \\ge F_\\alpha(n_2-1, n_1-1) $$

---

## F 检验与 t 检验的关系

两样本合并方差 t 检验的前提是两总体方差相等。实际分析流程：

1. 先用 **F 检验**判断 $\\sigma_1^2 = \\sigma_2^2$
2. 若不拒绝 → 用**合并 t 检验**
3. 若拒绝 → 用 **Welch t 检验**

---

## 例题 1：方差是否相等（双侧）

$n_1 = 12$，$n_2 = 10$，$s_1^2 = 64$，$s_2^2 = 49$，$\\alpha = 0.05$。

$$ F = \\frac{64}{49} = 1.31 $$

$F_{0.025}(11, 9) = 3.78$，$1.31 < 3.78$，**不拒绝** $H_0$，可认为方差相等，后续可用合并 t 检验。

---

## 例题 2：方差不等（双侧）

$n_1 = 15$，$n_2 = 12$，$s_1^2 = 100$，$s_2^2 = 25$。

用大方差作分子：$F = \\frac{100}{25} = 4.0$，$df = (14, 11)$。

$F_{0.025}(14, 11) = 3.53$，$4.0 > 3.53$，**拒绝** $H_0$，方差显著不等，应使用 Welch t 检验。

---

## 例题 3：单侧检验

$H_0: \\sigma_1^2 \\le \\sigma_2^2$ vs $H_1: \\sigma_1^2 > \\sigma_2^2$。$s_1^2 = 50$，$s_2^2 = 20$，$n_1 = 11$，$n_2 = 9$。

$$ F = \\frac{50}{20} = 2.5 $$

$F_{0.05}(10, 8) = 3.07$，$2.5 < 3.07$，**不拒绝** $H_0$。

---

## 常见误区

**误区 1**：F 检验分子分母随意放置。
> 正确：$H_0$ 下 $F = S_1^2/S_2^2$ 对应 $F(n_1-1, n_2-1)$；单侧时注意方向。

**误区 2**：F 检验显著后直接比较 $S_1^2$ 与 $S_2^2$ 大小下结论。
> 正确：F 检验只判断"是否相等"，不等时哪个更大需看 $F$ 值与 1 的关系。

**误区 3**：双侧 F 检验忘记右偏分布的不对称性。
> 正确：用"大方差作分子"技巧可简化为单侧上尾判断。

<ManimAnimation src="/videos/ch08/f_test.mp4" title="F检验的原理与应用动画" />

<InteractiveDistribution type="normal" mean={0} stdDev={1} />
`
    },
    {
      id: "8.13",
      title: "检验的功效与样本量确定",
      content: `
## 检验功效的定义

**检验功效 (Power of a Test)** 定义为当备择假设 $H_1$ 为真时，正确拒绝原假设 $H_0$ 的概率：

$$ \\text{Power} = 1 - \\beta = P(\\text{拒绝 } H_0 \\mid H_1 \\text{ 为真}) $$

其中 $\\beta$ 为第二类错误（纳伪）概率。功效越高，检验发现真实效应的能力越强。

> **实践标准**：通常要求功效 $\\ge 0.80$（即 $\\beta \\le 0.20$），重要研究可能要求 $0.90$ 甚至更高。

---

## 影响功效的四大因素

| 因素 | 增大功效 | 减小功效 | 说明 |
| :--- | :--- | :--- | :--- |
| 样本量 $n$ | $n \\uparrow$ | $n \\downarrow$ | 最直接的手段 |
| 显著性水平 $\\alpha$ | $\\alpha \\uparrow$ | $\\alpha \\downarrow$ | 增大 $\\alpha$ 扩大拒绝域 |
| 效应量 $\\delta$ | $|\\mu - \\mu_0| \\uparrow$ | $|\\mu - \\mu_0| \\downarrow$ | 真实差异越大越易检出 |
| 总体标准差 $\\sigma$ | $\\sigma \\downarrow$ | $\\sigma \\uparrow$ | 波动小则更易区分 |

在 $n$ 和 $\\alpha$ 固定时，**唯一同时减小 $\\alpha$ 和 $\\beta$ 的方法是增大 $n$**（见 8.4 节）。

---

## 功效的几何解释

考虑 $H_0: \\mu = \\mu_0$ vs $H_1: \\mu = \\mu_1$（$\\mu_1 > \\mu_0$）的 Z 检验：

- $H_0$ 下：$\\bar{X} \\sim N(\\mu_0, \\sigma^2/n)$
- $H_1$ 下：$\\bar{X} \\sim N(\\mu_1, \\sigma^2/n)$

拒绝域（右侧）为 $\\bar{X} > c$。功效 = $H_1$ 分布下 $\\bar{X} > c$ 的面积 = $1 - \\beta$。

---

## 样本量公式推导（Z 检验，双侧）

要检测均值差 $|\\mu - \\mu_0| \\ge \\delta$，给定 $\\alpha$ 和功效 $1-\\beta$：

<FormulaSteps formula="n = \\left( \\frac{z_{\\alpha/2} + z_\\beta}{\\delta / \\sigma} \\right)^2" steps={[
  "在 $H_0$ 下，双侧拒绝域边界：$\\bar{X} = \\mu_0 \\pm z_{\\alpha/2} \\cdot \\sigma/\\sqrt{n}$。",
  "考虑 $H_1: \\mu = \\mu_0 + \\delta$（右侧情形，双侧取最不利情况）。",
  "功效 = $P(\\bar{X} > \\mu_0 + z_{\\alpha/2} \\sigma/\\sqrt{n} \\mid \\mu = \\mu_0 + \\delta)$。",
  "标准化：$1 - \\Phi\\left(z_{\\alpha/2} - \\frac{\\delta\\sqrt{n}}{\\sigma}\\right) = 1 - \\beta$。",
  "令 $z_\\beta = \\frac{\\delta\\sqrt{n}}{\\sigma} - z_{\\alpha/2}$，解得 $n = \\left(\\frac{z_{\\alpha/2} + z_\\beta}{\\delta/\\sigma}\\right)^2$。"
]} />

**单侧检验**时，公式中 $z_{\\alpha/2}$ 换为 $z_\\alpha$。

---

## 例题 1：双侧样本量计算

检测总体均值与 $\\mu_0 = 100$ 相差至少 $\\delta = 5$，已知 $\\sigma = 20$。要求 $\\alpha = 0.05$，功效 $1-\\beta = 0.80$。

$z_{0.025} = 1.96$，$z_{0.20} = 0.84$（因 $1-\\beta = 0.80$，$\\beta = 0.20$）。

$$ n = \\left( \\frac{1.96 + 0.84}{5/20} \\right)^2 = \\left( \\frac{2.80}{0.25} \\right)^2 = (11.2)^2 = 125.44 $$

取 **$n = 126$**（向上取整）。

---

## 例题 2：单侧样本量计算

$H_1: \\mu > \\mu_0$，要检测 $\\delta = 3$ 的增幅，$\\sigma = 10$，$\\alpha = 0.05$，功效 $0.90$。

$z_{0.05} = 1.645$，$z_{0.10} = 1.28$（$\\beta = 0.10$）。

$$ n = \\left( \\frac{1.645 + 1.28}{3/10} \\right)^2 = \\left( \\frac{2.925}{0.3} \\right)^2 = (9.75)^2 = 95.06 $$

取 **$n = 96$**。

---

## 功效曲线 (Power Curve)

以样本量 $n$ 或效应量 $\\delta$ 为横轴，功效 $1-\\beta$ 为纵轴作图：

- $n$ 增大 → 功效单调上升
- $\\delta$ 增大 → 功效单调上升
- $\\alpha$ 增大 → 整条曲线向上平移

功效曲线帮助研究者在固定预算下选择合理的 $n$。

---

## 操作特征曲线 (OC Curve)

**OC 曲线**描述在不同参数值下 $P(\\text{不拒绝 } H_0)$ 的变化：

- 当 $H_0$ 为真时，OC 曲线高度为 $1-\\alpha$（正确不拒绝的概率）
- 当 $H_1$ 为真时，OC 曲线高度为 $\\beta$（第二类错误概率）

OC 曲线常用于**抽样检验方案**设计（如工业验收抽样）。

---

## t 检验的样本量（近似）

当 $\\sigma$ 未知时，用 t 检验，样本量公式需用 t 分位数近似。大样本下仍可用 Z 公式：

$$ n \\approx \\left( \\frac{t_{\\alpha/2}(n-1) + t_\\beta(n-1)}{\\delta / s} \\right)^2 $$

实际中常用迭代或软件（G*Power、R pwr 包）计算。

---

## 实践指南

1. **研究设计阶段**进行功效分析，避免样本不足导致"假阴性"
2. **报告**效应量 $\\delta$、$n$、$\\alpha$、功效或 $\\beta$
3. **效应量**应基于先验研究或 pilot 数据，而非随意设定
4. 若功效 $< 0.80$，应增大 $n$ 或放宽 $\\alpha$（需权衡）

---

## 常见误区

**误区 1**：不显著 = 无效应。
> 正确：可能是样本量太小，功效不足，无法检出真实效应。

**误区 2**：样本量越大越好，无上限。
> 正确：过大 $n$ 可能检出无实际意义的微小差异；需结合效应量。

**误区 3**：事后计算功效（post-hoc power）解释不显著结果。
> 正确：事后功效与 p 值存在数学对偶，信息冗余；应报告置信区间和效应量。

---

## 提示

- 考研中样本量公式以 **Z 检验双侧** 为主，记住 $n = (z_{\\alpha/2} + z_\\beta)^2 \\sigma^2 / \\delta^2$
- $z_\\beta$ 由功效 $1-\\beta$ 查表：如 $1-\\beta = 0.80$ 则 $z_{0.20} = 0.84$；$1-\\beta = 0.90$ 则 $z_{0.10} = 1.28$

---

## 本章知识脉络总结

| 节 | 核心内容 | 关键统计量 |
| :---: | :--- | :--- |
| 8.1–8.4 | 基本思想、假设建立、$\\alpha$、两类错误 | — |
| 8.5–8.6 | 检验统计量、拒绝域、p 值 | 通用框架 |
| 8.7 | 单总体均值，$\\sigma^2$ 已知 | $Z$ |
| 8.8 | 单总体均值，$\\sigma^2$ 未知 | $t$ |
| 8.9 | 两总体均值比较 | $Z$, $t$, Welch |
| 8.10 | 配对样本 | 配对 $t$ |
| 8.11 | 单总体方差 | $\\chi^2$ |
| 8.12 | 两总体方差比 | $F$ |
| 8.13 | 功效与样本量 | $n = (z_{\\alpha/2}+z_\\beta)^2\\sigma^2/\\delta^2$ |

> **考研复习建议**：熟记五步法；根据"已知/未知方差""单/双样本""是否配对"选择检验；注意 $\\chi^2$ 与 $F$ 分布的**非对称性**及"大方差作分子"技巧；功效公式要会推导与应用。

---

## 假设检验公式速查（正态总体）

**单样本均值**：
- $\\sigma^2$ 已知：$Z = \\frac{\\bar{X}-\\mu_0}{\\sigma/\\sqrt{n}}$
- $\\sigma^2$ 未知：$t = \\frac{\\bar{X}-\\mu_0}{S/\\sqrt{n}} \\sim t(n-1)$

**两样本均值**（$\\sigma_1^2=\\sigma_2^2$ 未知）：
- $t = \\frac{(\\bar{X}-\\bar{Y})-\\delta}{S_p\\sqrt{1/n_1+1/n_2}} \\sim t(n_1+n_2-2)$

**配对样本**：对 $D_i=X_i-Y_i$ 做单样本 t 检验。

**单总体方差**：$\\chi^2 = \\frac{(n-1)S^2}{\\sigma_0^2} \\sim \\chi^2(n-1)$

**两方差比**：$F = \\frac{S_1^2}{S_2^2} \\sim F(n_1-1,n_2-1)$（$H_0: \\sigma_1^2=\\sigma_2^2$）

<ManimAnimation src="/videos/ch08/power_analysis.mp4" title="统计功效与样本量的关系动画" />

<InteractiveDistribution type="normal" mean={0} stdDev={1} />
`
    }
  ]
};
