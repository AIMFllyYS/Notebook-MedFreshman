export const ch06Content = {
  sections: [
    {
      id: "6.1",
      title: "随机样本",
      content: `
### 1. 总体与样本

数理统计学是一门搜集、整理和分析带有随机性的数据的科学。数理统计与概率论是密切相关的两个分支，概率论是研究“已知概率分布求事件特征”的演绎科学，而数理统计则是研究“已知样本数据推断总体概率分布”的归纳科学。

#### (1) 总体 (Population)
在数理统计中，我们将研究对象的全体称为**总体**。总体中的每一个研究对象称为**个体**。
由于每个个体的数据特征具有一定的随机性，因此我们通常将总体看作是一个随机变量（或随机向量），记为 $X$。总体的分布函数记为 $F(x)$，相应的概率密度或分布律称为总体分布。

#### (2) 样本 (Sample)
为了解总体的性质，我们需要从总体中抽取一部分个体进行观测，这部分被抽取的个体称为**样本**。
如果抽取的样本数量为 $n$，则样本可以表示为 $n$ 个相互关联的随机变量 $X_1, X_2, \\dots, X_n$。一次具体观测得到的实数值 $x_1, x_2, \\dots, x_n$ 称为**样本值**或**观测值**。

---

### 2. 简单随机样本 (Simple Random Sample)

为了使样本能够很好地代表总体，并且在数学上易于处理，最常用且最理想的抽样方式是“简单随机抽样”。

#### 定义
若总体 $X$ 的样本 $X_1, X_2, \\dots, X_n$ 满足以下两个条件：
1. **同分布性 (Identically Distributed)**：每个 $X_i$ 与总体 $X$ 具有完全相同的概率分布，即 $X_i \\sim F(x), i=1,2,\\dots,n$。
2. **相互独立性 (Independent)**：随机变量 $X_1, X_2, \\dots, X_n$ 之间是相互独立的。

则称 $X_1, X_2, \\dots, X_n$ 为来自总体 $X$ 的**简单随机样本**，简称为**样本**，其个数 $n$ 称为**样本容量**。

#### 简单随机样本的联合分布
由于简单随机样本的独立同分布 (i.i.d.) 特性，其联合分布函数可写为边缘分布函数的乘积：
$$ F(x_1, x_2, \\dots, x_n) = \\prod_{i=1}^n F(x_i) $$

- 若总体 $X$ 为连续型随机变量，概率密度为 $f(x)$，则样本 $X_1, \\dots, X_n$ 的联合概率密度为：
  $$ f(x_1, x_2, \\dots, x_n) = \\prod_{i=1}^n f(x_i) $$
- 若总体 $X$ 为离散型随机变量，分布律为 $P(X=x)$，则样本 $X_1, \\dots, X_n$ 的联合分布律为：
  $$ P(X_1=x_1, X_2=x_2, \\dots, X_n=x_n) = \\prod_{i=1}^n P(X=x_i) $$

---

### 3. 统计量 (Statistic)

样本是包含总体信息的随机变量，但样本信息通常较为分散。为了提取有用的信息以进行统计推断，需要对样本进行加工处理，即构造关于样本的函数。

#### 定义
设 $X_1, X_2, \\dots, X_n$ 是来自总体 $X$ 的一个样本，$g(X_1, X_2, \\dots, X_n)$ 是一个多元实值连续函数。若 $g$ 中**不含有任何未知的总体参数**，则称随机变量
$$ Y = g(X_1, X_2, \\dots, X_n) $$
为该样本的一个**统计量**。

- **重要提醒**：统计量的关键在于“已知性”。例如，如果总体均值 $\\mu$ 是未知的，则 $\\bar{X} - \\mu$ 不是统计量；而样本均值 $\\bar{X}$ 是统计量。如果总体均值 $\\mu$ 是已知的（例如标准正态分布中的 $\\mu = 0$），则此时 $\\bar{X} - \\mu$ 是统计量。

---

### 4. 常用统计量及其数理性质

#### (1) 样本均值 (Sample Mean)
样本均值代表了样本观测值的算术平均：
$$ \\bar{X} = \\frac{1}{n} \\sum_{i=1}^n X_i $$
- **性质**：若总体 $X$ 的期望为 $E(X) = \\mu$，方差为 $D(X) = \\sigma^2$，则：
  $$ E(\\bar{X}) = \\mu, \\quad D(\\bar{X}) = \\frac{\\sigma^2}{n} $$

#### (2) 样本方差 (Sample Variance)
样本方差是衡量样本波动程度的核心指标：
$$ S^2 = \\frac{1}{n-1} \\sum_{i=1}^n (X_i - \\bar{X})^2 = \\frac{1}{n-1} \\left( \\sum_{i=1}^n X_i^2 - n\\bar{X}^2 \\right) $$
样本方差的算术平方根 $S = \\sqrt{S^2}$ 称为**样本标准差**。
- **重要问题**：为什么样本方差的分母是 $n-1$ 而不是 $n$？
  这是因为在用样本均值 $\\bar{X}$ 代替未知总体均值 $\\mu$ 时，消耗了一个自由度。如果我们除以 $n-1$，则样本方差 $S^2$ 的数学期望正好等于总体方差 $\\sigma^2$，即 $S^2$ 是总体方差的**无偏估计**。

下面我们通过分步推导展示这一结论의数学证明：

<FormulaSteps 
  formula="E(S^2) = \\sigma^2" 
  title="样本方差无偏性的严格数学证明"
  steps={[
    "首先，展开样本方差定义式中的分子部分：\\\\sum_{i=1}^n (X_i - \\\\bar{X})^2 = \\\\sum_{i=1}^n (X_i^2 - 2X_i\\\\bar{X} + \\\\bar{X}^2) = \\\\sum_{i=1}^n X_i^2 - 2n\\\\bar{X}^2 + n\\\\bar{X}^2 = \\\\sum_{i=1}^n X_i^2 - n\\\\bar{X}^2。",
    "分别求各项的数学期望。对第一项：由于每个 $X_i$ 与总体同分布，故 $E(X_i^2) = D(X_i) + [E(X_i)]^2 = \\\\sigma^2 + \\\\mu^2$。因此 \\\\sum_{i=1}^n E(X_i^2) = n(\\\\sigma^2 + \\\\mu^2)。",
    "对第二项：因为样本均值 \\\\bar{X} 的期望 $E(\\\\bar{X}) = \\\\mu$，方差 $D(\\\\bar{X}) = \\\\sigma^2 / n$，可得 $E(\\\\bar{X}^2) = D(\\\\bar{X}) + [E(\\\\bar{X})]^2 = \\\\frac{\\\\sigma^2}{n} + \\\\mu^2$。",
    "将上述结果代入分子部分的期望中：$E\\\\left[ \\\\sum_{i=1}^n (X_i - \\\\bar{X})^2 \\\\right] = n(\\\\sigma^2 + \\\\mu^2) - n\\\\left(\\\\frac{\\\\sigma^2}{n} + \\\\mu^2\\\\right) = n\\\\sigma^2 - \\\\sigma^2 = (n-1)\\\\sigma^2$。",
    "最后，求样本方差 $S^2$ 的期望：$E(S^2) = E\\\\left[ \\\\frac{1}{n-1} \\\\sum_{i=1}^n (X_i - \\\\bar{X})^2 \\\\right] = \\\\frac{1}{n-1} E\\\\left[ \\\\sum_{i=1}^n (X_i - \\\\bar{X})^2 \\\\right] = \\\\frac{n-1}{n-1}\\\\sigma^2 = \\\\sigma^2$。由此证毕！"
  ]}
/>

#### (3) 样本未修正方差 (Sample Uncorrected Variance)
在某些中外文献或计算中，也会直接除以 $n$ 得到样本未修正方差（即样本二阶中心矩，记为 $S^{*2}$ 或 $B_2$）：
$$ S^{*2} = \\frac{1}{n} \\sum_{i=1}^n (X_i - \\bar{X})^2 $$
易知 $E(S^{*2}) = \\frac{n-1}{n} \\sigma^2$，所以它不是总体方差的无偏估计。两者转换关系为：
$$ S^2 = \\frac{n}{n-1} S^{*2} $$

#### (4) 样本原点矩与样本中心矩
- **样本 $k$ 阶原点矩**：
  $$ A_k = \\frac{1}{n} \\sum_{i=1}^n X_i^k, \\quad k=1, 2, \\dots $$
  当 $k=1$ 时，$A_1 = \\bar{X}$ 为样本均值。
- **样本 $k$ 阶中心矩**：
  $$ B_k = \\frac{1}{n} \\sum_{i=1}^n (X_i - \\bar{X})^2, \\quad k=1, 2, \\dots $$
  当 $k=2$ 时，$B_2 = S^{*2}$ 为样本未修正方差。
`
    },
    {
      id: "6.2",
      title: "直方图和箱线图",
      content: `
### 1. 频数/频率直方图

直方图是数理统计中用以整理、展示样本观测值分布规律的常用图形方法。

#### 构造步骤
1. **寻找范围**：找出样本值中的最大值 $x_{\\max}$ 和最小值 $x_{\\min}$，确定极差 $R = x_{\\max} - x_{\\min}$。
2. **确定组数与组距**：根据样本容量 $n$，确定分组个数 $k$（一般选 $5$ 到 $15$ 组），计算组距 $d = R/k$。
3. **划分组区间**：把区间 $[x_{\\min}, x_{\\max}]$ 分成 $k$ 个相邻的小区间：
   $$ [a_0, a_1), [a_1, a_2), \\dots, [a_{k-1}, a_k] $$
   为了使所有样本点都能落入某一个区间内，通常使起点 $a_0$ 稍小于 $x_{\\min}$，终点 $a_k$ 稍大于 $x_{\\max}$。
4. **统计频数与频率**：清点落入每个组区间内的样本个数（频数）$f_i$。计算各组的频率 $f_i/n$。
5. **绘制直方图**：在直角坐标系中，横轴为样本观测值，纵轴为**频率/组距**。在每个组区间上画高度为 $\\frac{f_i}{n \\cdot d}$ 的矩形。

- **直方图的关键性质**：
  由于矩形的宽为组距 $d$，高为频率/组距 $\\frac{f_i}{n \\cdot d}$，因此每个矩形的面积正好等于该组的频率 $\\frac{f_i}{n}$。
  所有矩形的总面积等于 1：
  $$ \\sum_{i=1}^k \\left( \\frac{f_i}{n \\cdot d} \\cdot d \\right) = \\sum_{i=1}^k \\frac{f_i}{n} = 1 $$
  当样本量 $n \\to \\infty$ 且组距 $d \\to 0$ 时，频率直方图的外轮廓折线会趋近于总体 $X$ 的概率密度曲线 $f(x)$。

---

### 2. 箱线图 (Boxplot)

箱线图又称盒须图，是利用数据中的五个关键指标来直观展示数据分布的形态、分散度以及异常值的图表。

#### 五数概括 (Five-number Summary)
箱线图的核心构造依赖于这五个统计特征值：
1. **最小值 (Minimum)**：样本中的非异常最小值。
2. **下四分位数 (Lower Quartile, $Q_1$)**：将样本数据按大小升序排列后，排在第 $25\\%$ 位置的值。
3. **中位数 (Median, $Q_2$)**：排在第 $50\\%$ 位置的值。
4. **上四分位数 (Upper Quartile, $Q_3$)**：排在第 $75\\%$ 位置的值。
5. **最大值 (Maximum)**：样本中的非异常最大值。

#### 四分位距 (Interquartile Range, IQR)
定义上四分位数与下四分位数的差值为四分位距：
$$ \\text{IQR} = Q_3 - Q_1 $$
It反映了数据中间 $50\\%$ 观测值的波动范围。

#### 异常值的判断标准 (Tukey's Fences)
在箱线图中，数据点的正常范围定义为：
$$ [Q_1 - 1.5\\text{IQR}, \\ Q_3 + 1.5\\text{IQR}] $$
- **疑似异常值**：凡是小于 $Q_1 - 1.5\\text{IQR}$ 或大于 $Q_3 + 1.5\\text{IQR}$ 的观测值，被判定为**异常值**，并在箱线图上作为独立的散点画出。
- **极端异常值**：落在 $[Q_1 - 3.0\\text{IQR}, \\ Q_3 + 3.0\\text{IQR}] $ 范围之外的观测值。
`
    },
    {
      id: "6.3",
      title: "抽样分布",
      content: `
### 1. 抽样分布的基本概念

数理统计的重要任务之一是根据样本去推断总体的分布。为此需要构造统计量，并确定统计量的概率分布。
**抽样分布**即是指**统计量的概率分布**。由于统计量是样本随机变量的函数，即使总体分布很简单，其抽样分布也可能非常复杂。

在数理统计学中，有三个基于正态总体的重要分布在推断统计中起着基石作用，分别被称为“三大抽样分布”：卡方分布、学生氏 $t$ 分布和 $F$ 分布。

---

### 2. 三大常用抽样分布

<ManimAnimation src="/videos/ch06/t_distribution.mp4" title="三大抽样分布（卡方、t、F 分布）的几何构造物理意义" />

#### (1) $\\chi^2$ 分布（卡方分布）

##### 定义
设 $X_1, X_2, \\dots, X_n$ 是相互独立的随机变量，且都服从标准正态分布 $N(0, 1)$。则随机变量
$$ Y = \\sum_{i=1}^n X_i^2 $$
所服从的分布称为**自由度为 $n$ 的 $\\chi^2$ 分布**，记作 $Y \\sim \\chi^2(n)$。
自由度是指独立的正态随机变量个数。

##### 性质
1. **分布范围**：若 $Y \\sim \\chi^2(n)$，则其概率密度只在 $[0, \\infty)$ 有值。当自由度 $n \\le 2$ 时，密度曲线单调递减；当 $n > 2$ 时，曲线呈单峰右偏（偏态）形状。随着自由度 $n$ 的增大，卡方分布的对称性增加，由中心极限定理，当 $n \\to \\infty$ 时，其极限分布为正态分布。
2. **可加性 (Additivity)**：若 $Y_1 \\sim \\chi^2(n_1)$，$Y_2 \\sim \\chi^2(n_2)$，且 $Y_1$ 与 $Y_2$ 相互独立，则：
   $$ Y_1 + Y_2 \\sim \\chi^2(n_1 + n_2) $$
3. **均值与方差**：若 $Y \\sim \\chi^2(n)$，则：
   $$ E(Y) = n, \\quad D(Y) = 2n $$
   证明：因为 $X_i \\sim N(0, 1)$，所以 $E(X_i) = 0$，$D(X_i) = 1$。
   $$ E(X_i^2) = D(X_i) + [E(X_i)]^2 = 1 $$
   根据正态分布性质，$E(X_i^4) = 3$。所以：
   $$ D(X_i^2) = E(X_i^4) - [E(X_i^2)]^2 = 3 - 1^2 = 2 $$
   利用期望和方差的线性加和性（独立性），有：
   $$ E(Y) = \\sum_{i=1}^n E(X_i^2) = n, \\quad D(Y) = \\sum_{i=1}^n D(X_i^2) = 2n $$

#### (2) $t$ 分布（学生氏分布）

##### 定义
设 $X \\sim N(0, 1)$，$Y \\sim \\chi^2(n)$，且 $X$ 与 $Y$ 相互独立。则随机变量
$$ T = \\frac{X}{\\sqrt{Y/n}} $$
所服从的分布称为**自由度为 $n$ 的 $t$ 分布**，记作 $T \\sim t(n)$。

##### 性质
1. **对称性与形态**：$t$ 分布的概率密度函数 $f(t)$ 关于 $t=0$ 对称。其曲线形态与标准正态分布的钟形曲线非常相似，但 $t$ 分布的尾部较“厚”，即对于较大的自变量值，$t$ 分布的概率密度大于标准正态分布。这是由于分母中的卡方变量 $Y$ 带来了额外的不确定性。
2. **渐近收敛性**：当自由度 $n \\to \\infty$ 时，$t(n)$ 分布的极限分布就是标准正态分布 $N(0, 1)$。在实际应用中，当 $n \\ge 30$ 时，常可用标准正态分布来近似。
3. **均值与方差**：
   - 当自由度 $n > 1$ 时，$E(T) = 0$。
   - 当自由度 $n > 2$ 时，$D(T) = \\frac{n}{n-2}$。注意到 $\\frac{n}{n-2} > 1$，这正是“厚尾”特性的定量化表现。

#### (3) $F$ 分布

##### 定义
设 $U \\sim \\chi^2(n_1)$，$V \\sim \\chi^2(n_2)$，且 $U$ 与 $V$ 相互独立。则随机变量
$$ F = \\frac{U/n_1}{V/n_2} $$
所服从的分布称为**自由度为 $(n_1, n_2)$ 的 $F$ 分布**，记作 $F \\sim F(n_1, n_2)$。其中 $n_1$ 称为第一自由度（分子自由度），$n_2$ 称为第二自由度（分母自由度）。

##### 性质
1. **倒数性质**：若 $F \\sim F(n_1, n_2)$，则：
   $$ \\frac{1}{F} \\sim F(n_2, n_1) $$
   该性质在计算假设检验和置信区间的临界值时非常关键。
2. **与 $t$ 分布的关系**：若 $T \\sim t(n)$，则其平方：
   $$ T^2 \\sim F(1, n) $$
   证明：$T^2 = \\frac{X^2}{Y/n}$。由于 $X \\sim N(0, 1)$，所以 $X^2 \\sim \\chi^2(1)$。而 $Y \\sim \\chi^2(n)$ 且与 $X^2$ 独立，故根据 $F$ 分布定义有 $T^2 \\sim F(1, n)$。

---

### 3. 分位数 (Quantile)

分位数是用来确定概率分布临界值的重要概念，是假设检验与区间估计的基础。

#### 上 $\alpha$ 分位数
设 $X$ 是一个随机变量，其分布函数为 $F(x)$，对于给定的 $\alpha$ ($0 < \\alpha < 1$)，称满足：
$$ P(X > x_\\alpha) = 1 - F(x_\\alpha) = \\alpha $$
的数 $x_\\alpha$ 为该随机变量 $X$（或该分布）的**上 $\alpha$ 分位数**。

- **标准正态分布的上 $\alpha$ 分位数**：记为 $u_\\alpha$。由于对称性，显然有：
  $$ u_{1-\\alpha} = -u_\\alpha $$
- **$t$ 分布的上 $\alpha$ 分位数**：记为 $t_\\alpha(n)$。由于其对称性，同样有：
  $$ t_{1-\\alpha}(n) = -t_\\alpha(n) $$
- **$\\chi^2$ 分布的上 $\alpha$ 分位数**：记为 $\\chi^2_\\alpha(n)$。
- **$F$ 分布的上 $\alpha$ 分位数**：记为 $F_\\alpha(n_1, n_2)$。利用倒数性质，有：
  $$ F_{1-\\alpha}(n_1, n_2) = \\frac{1}{F_\\alpha(n_2, n_1)} $$

---

### 4. 正态总体的抽样分布定理

当总体服从正态分布时，其样本均值和样本方差具有极其优美且重要的代数与分布性质。以下定理是整个假设检验与参数估计的理论大厦之基石。

#### (1) 单个正态总体 $N(\\mu, \\sigma^2)$ 的抽样定理
设 $X_1, X_2, \\dots, X_n$ 是来自总体 $N(\\mu, \\sigma^2)$ 的一个样本，$\\bar{X}$ 和 $S^2$ 分别是样本均值和样本方差。

- **定理一（样本均值的分布）**：
  $$ \\bar{X} \\sim N\\left(\\mu, \\frac{\\sigma^2}{n}\\right) $$
  由此标准化可得：
  $$ \\frac{\\bar{X} - \\mu}{\\sigma/\\sqrt{n}} \\sim N(0, 1) $$
- **定理二（样本均值与样本方差的独立性）**：
  $$ \\bar{X} \\text{ 与 } S^2 \\text{ 相互独立} $$
  > **重要警告**：这是正态总体的特有属性！对于非正态总体，即使同分布，$\\bar{X}$ 与 $S^2$ 通常也不独立。
- **定理三（样本方差的卡方分布）**：
  $$ \\frac{(n-1)S^2}{\\sigma^2} = \\frac{\\sum_{i=1}^n (X_i - \\bar{X})^2}{\\sigma^2} \\sim \\chi^2(n-1) $$
- **定理四（未知总体方差下的均值分布，$t$ 分布）**：
  $$ \\frac{\\bar{X} - \\mu}{S/\\sqrt{n}} \\sim t(n-1) $$

定理四的成立可以通过下面的分步推导展示其几何与代数转换：

<FormulaSteps 
  formula="\\frac{\\bar{X} - \\mu}{S/\\sqrt{n}} \\sim t(n-1)" 
  title="单个正态总体 t 统计量的推导步骤"
  steps={[
    "首先，构造标准正态分布统计量 $Z = \\\\frac{\\\\bar{X} - \\\\mu}{\\\\sigma / \\\\sqrt{n}} \\\\sim N(0, 1)$。这描述了均值在总体标准差已知时的波动规律。",
    "然后，构造服从卡方分布的统计量 $Y = \\\\frac{(n-1)S^2}{\\\\sigma^2} \\\\sim \\\\chi^2(n-1)$。这刻画了样本方差围绕总体方差的波动程度。",
    "根据正态总体的核心结论，样本均值 $\\\\bar{X}$ 与样本方差 $S^2$ 相互独立，因此随机变量 $Z$ 与 $Y$ 相互独立。",
    "将独立变量 $Z$ 和 $Y$ 按照学生氏 $t$ 分布的定义进行拼装，构造比值统计量：$T = \\\\frac{Z}{\\\\sqrt{Y / (n-1)}}$，依据定义，其服从自由度为 $n-1$ 的 $t$ 分布。",
    "代入 $Z$ 和 $Y$ 并消去分子分母中的总体标准差 $\\\\sigma$：$T = \\\\frac{\\\\frac{\\\\bar{X} - \\\\mu}{\\\\sigma / \\\\sqrt{n}}}{\\\\sqrt{\\\\frac{(n-1)S^2}{\\\\sigma^2} / (n-1)}} = \\\\frac{\\\\bar{X} - \\\\mu}{S / \\\\sqrt{n}}$，即得 $\\\\frac{\\\\bar{X} - \\\\mu}{S / \\\\sqrt{n}} \\\\sim t(n-1)$。证毕！"
  ]}
/>

#### (2) 两个独立正态总体的抽样定理
设总体 $X \\sim N(\\mu_1, \\sigma_1^2)$，样本为 $X_1, \\dots, X_{n_1}$，均值为 $\\bar{X}$，方差为 $S_1^2$；
总体 $Y \\sim N(\\mu_2, \\sigma_2^2)$，样本为 $Y_1, \\dots, Y_{n_2}$，均值为 $\\bar{Y}$，方差为 $S_2^2$。
且两样本相互独立。

- **定理五（方差比与 $F$ 分布）**：
  $$ \\frac{S_1^2 / \\sigma_1^2}{S_2^2 / \\sigma_2^2} \\sim F(n_1 - 1, n_2 - 1) $$
  特别地，如果两总体方差相等，即 $\\sigma_1^2 = \\sigma_2^2$，则：
  $$ \\frac{S_1^2}{S_2^2} \\sim F(n_1 - 1, n_2 - 1) $$
- **定理六（均值差与 $t$ 分布，方差相等但未知）**：
  若 $\\sigma_1^2 = \\sigma_2^2 = \\sigma^2$，定义联合样本方差（Pooled Variance）为：
  $$ S_w^2 = \\frac{(n_1 - 1)S_1^2 + (n_2 - 1)S_2^2}{n_1 + n_2 - 2} $$
  则有：
  $$ \\frac{(\\bar{X} - \\bar{Y}) - (\\mu_1 - \\mu_2)}{S_w \\sqrt{\\frac{1}{n_1} + \\frac{1}{n_2}}} \\sim t(n_1 + n_2 - 2) $$
`
    }
  ]
};
