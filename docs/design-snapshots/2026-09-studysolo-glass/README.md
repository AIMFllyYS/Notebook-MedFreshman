# 2026-09 · StudySolo Glass 玻璃拟态设计快照

> **状态：探索存档，未被产品采纳。体系尚不成熟，禁止作为新开发的设计或实现参考。**

## 来源

2026 年 9 月玻璃拟态 / macOS 风格设计探索的完整产出，原位于仓库根目录：

- `.design_library/` → 现 `design-library/`
- `studysolo-glass-mockup/` → 现 `mockup/`

因不进入当前产品迭代、也不希望新开发误引用，2026-09-18 整体移入本快照目录存档。

## 目录内容

- `design-library/studysolo-glass/`：设计系统本体。三主题（深炭 / 雾蓝 / 奶油）设计令牌、6 个组件契约、Figma Tokens Studio 文件、组件预览页与 dashboard UI Kit。
- `design-library/.tmp/`：生成过程中的阶段报告与构建脚本，仅作过程记录。
- `mockup/`：13 屏设计稿（2 个核心屏 × 3 主题、2 张基础规范页、4 张 Agent 状态页、1 张玻璃性能特化页），含 `.design` 画板节点与页面 HTML。

## 备注

- 页面 HTML 自包含、可直接在浏览器打开；页面间相对链接仍然有效。
- `mockup/studysolo-glass-mockup.design` 中的设计库路径已随本次移动更新；`runtime-*.json` / `validation-report.json` 内嵌的绝对路径仍为移动前的工作区路径，属存档元数据，不再维护。
- 若未来重启该方向，应从现行产品现状重新评估，而不是直接沿用本快照。
