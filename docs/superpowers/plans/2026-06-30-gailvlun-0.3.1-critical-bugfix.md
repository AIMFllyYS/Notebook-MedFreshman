# Gailvlun 0.3.1 Critical Bugfix Rollout

## Summary

Work directly on `master` to repair the current `0.3.1` desktop regressions. Keep the version number unchanged, commit after each phase, rebuild the Windows desktop artifacts, overwrite the existing `v0.3.1` release assets, and update the release notes with the fixed critical issues.

## Ordered Implementation

1. Baseline on `master`
   - Confirm branch, working tree, package version, existing desktop assets, and release tag/assets.
   - Run encoding, TypeScript, and current test gates.
   - Commit this plan as `docs: plan 0.3.1 critical bugfix rollout`.

2. Right panel white screen hardening
   - Add a shared right-panel tab boundary with loading, error, retry, and diagnostics states.
   - Ensure AI, video, interactive, and browser tabs never fail as silent blank panels.
   - Add desktop/source smoke coverage for right-side tab rendering.
   - Commit as `fix(desktop): harden right panel tab rendering`.

3. Custom API reasoning and OpenAI compatibility
   - Parse custom reasoning streams from configurable and common provider fields.
   - Add custom model settings for reasoning field, thinking request style, and image API style.
   - Avoid sending tools to models that do not support tools, while preserving OpenAI image attachment parts.
   - Commit as `fix(ai): normalize custom provider reasoning and capabilities`.

4. HTML artifact, SVG, and image mode
   - HTML interactive generation uses the model selected when generation is requested.
   - If the selected model is an image model, show a clear unsupported message instead of calling `/api/artifact`.
   - If the selected model is text/custom text, call `/api/artifact` with that model and provider metadata.
   - Require complete `<svg>` roots in prompts and tolerate bare SVG children by wrapping them safely.
   - In image mode, force `generateImage` and use the selected image model.
   - Commit as `fix(artifacts): respect selected model for html svg and image flows`.

5. Conversation context and 80 percent policy
   - Use a stable per-session context budget for the tab context ring.
   - Keep input enabled over 80 percent, truncate only the prompt sent to the model, and show a persistent warning.
   - Deduplicate dynamically loaded large tool context by context key.
   - Commit as `fix(context): keep sessions usable with token-aware truncation`.

6. Integration and desktop verification
   - Run encoding, TypeScript, unit, Vitest, and added regression tests.
   - Build desktop `0.3.1`, check standalone route manifest, probe packaged `/api/artifact`, and smoke the unpacked exe.
   - Commit as `test: cover 0.3.1 desktop critical regressions`.

7. Release refresh
   - Keep `package.json` at `0.3.1`.
   - Regenerate setup, portable, updater metadata, and blockmap artifacts.
   - Confirm `v0.3.1`, upload assets with `--clobber`, edit release notes, and verify remote asset metadata.
   - Commit as `chore(release): refresh 0.3.1 desktop notes`.

## Compatibility Requirements

- Do not change the desktop user data directory, default port, startup entry, or API key encryption path.
- Preserve existing sessions and settings through setup-based overwrite upgrade.
- Keep Chinese text valid UTF-8 and avoid replacement characters.
- Treat `dist-desktop` as generated output; do not rely on stale packaged source during source-level tests.

